# SDD ledger — plan: .superpowers/plans/2026-08-05-check-gate-project-config.md

Task 1: implementer done_with_concerns (oc_msgj53hj_4abbc1c9, minimax/MiniMax-M3), changeset accepted and committed a78547e (base 00856e3). Task reviewer (oc_msgjk5vr_1c1bbfd7, openai/gpt-5.6-luna --variant max): Needs fixes — 2 Important (missing @returns JSDoc on _resetProjectConfigCache; package.json engines.node >=16.9 stale vs smol-toml's >=18), 1 Minor (stale commit hash in report). Both Important findings verified against source before dispatching.
Task 1: fix round 1/5 (3 addressed, 0 open; oc_msgjx7vl_7504ce8d resumed same implementer session)
Task 1: re-review (oc_msgk3hvp_71c21727, openai/gpt-5.6-luna --variant max): all findings addressed, no new Critical/Important breakage. 1 new minor noted: task-1-report.md:154 records fix commit 8c93075 (implementer's in-sandbox hash) instead of landed head a80e9f9 — parked, cosmetic audit-trail only, not code.
Task 1: complete (commits 00856e3..a78547e..a80e9f9, 1 minor parked — stale audit-trail commit hash in task-1-report.md, cosmetic)
Task 2: first implementer dispatch (oc_msgk8fgb_b8fb5570) lost to an infra failure — the isolated dev daemon (pid 2953852, restarted ~17:06 EDT) crashed and respawned mid-task, tearing down the sandbox overlay's `upper/extra/check-gate-project-config-71e214ea` bind before extraction could run (changesetError: "bwrap: Can't find source path ... No such file or directory", diff: null). Worktree itself verified intact (git fsck clean, only expected dangling objects). Rejected the dead task, re-dispatched fresh: oc_msgkyqig_a8348b2b (same model, no session resume — prior sandbox unrecoverable). Root cause not yet identified; worth a taskferry issue if it recurs.
Task 2: fresh implementer oc_msgkyqig_a8348b2b done (incomplete: true — final message never printed the required `Status:` line, but diff verified real and matching its own claims: 13 files, +353/-41 real code+test diff, 994/994 tests per its report). Changeset accepted and committed a491d1e (base 29be084). Review package written: review-29be084..a491d1e.diff.
Task 2: reviewer (oc_msglqy5b_d758ce12): Needs fixes, root cause mixed per reviewer — 1 Important (acceptTaskChangeset param order swapped from plan-mandated (taskId, options, ctx) to (taskId, ctx, options), citing TS1016), 2 Minor (no e2e wiring test; missing Status: line, both deferred). Verified independently: this repo already has a working precedent (summarizeRequestFor/summarizeTaskFor at src/tasks.js:5318,5333) for a required-options-then-required-ctx signature that avoids TS1016 by destructuring defaults inside the function body instead of the parameter list — so this is an execution-error, not a genuine plan defect requiring human escalation. Fix round 1/5 dispatched pointing at that precedent: oc_msgm0mvv_6a4dd5e7 (resumed same implementer session).
Task 2: fix round 1/5 (1 addressed, 0 open; commit 402b539). Re-review (oc_msgm9o6r_b4477351): all findings addressed, no new Critical/Important breakage. 1 new minor: src/tasks.js:4688 bare `options;` statement (lint-satisfying no-op) is non-idiomatic — parked, intentional temporary stub until Task 6 consumes `force`.
Task 2: complete (commits 29be084..a491d1e..402b539, 3 minors parked — no e2e wiring test, prior implementer's missing Status: line, bare `options;` lint-satisfying stub)
Task 3: implementer done_with_concerns (oc_msgme4li_4f36ab6c, minimax/MiniMax-M3). Changeset accepted and committed 6b5959e (prerequisite tsc fix), 05c3b91 (feat, base 735b795). 997/997 unit tests after a fresh `npm install` (worktree's node_modules was stale/non-directory; the implementer's report showed 1 pre-existing failure that vanished after reinstall).
Task 3: reviewer (oc_msgnjnfd_20fb3c54, openai/gpt-5.6-luna --variant max): Needs fixes, root cause plan-error — dispatch-prompt-injection condition only checked `role === "dispatch"`, but the plan's Global Constraints (line 16) also require excluding `--no-overlay` dispatches; the task-3-brief.md snippet (written in a prior session) omitted this. Verified directly against the brief and plan text before entering the fix loop.
Task 3: fix round 1/5 (1 addressed, 0 open; oc_msgnxc4f_1dd6a6e3 resumed same implementer session, commit e503ea6 on top of 05c3b91). 998/998 tests, lint/typecheck clean. Re-review (oc_msgoawm8_3e2327ba, openai/gpt-5.6-luna --variant max): ADDRESSED, no new Critical/Important breakage.
Task 3: complete (commits 05c3b91..e503ea6, review clean after fix round 1)
Task 4: implementer done_with_concerns (oc_msgoivg2_e0378e0d, minimax/MiniMax-M3), commit 21517df — NOT accepted; self-flagged a Critical security bug in the brief's own `overlaps()` predicate before I could accept. Verified directly (standalone node check): `"/" + path.sep === "//"`, which never prefixes a normal absolute path, so `read_only_paths = ["/"]` (the brief's own stated canonical exploit) was NOT rejected as written. Dispatched a fix to the same implementer session before any review (oc_msgp7cwt_f48606be) rather than accepting the vulnerable version — its sandbox forked from the last-accepted base (73e173c) since the buggy changeset was never accepted, so the resumed session redid the task cleanly with the fix baked in from the start; no vulnerable commit ever entered git history. `incomplete: true` on this dispatch (no closing Status: line) — diff independently verified as real (childPrefix() root-case fix, literal `read_only_paths = ["/"]` regression test asserting the bwrap argv has no second `--ro-bind / /`) before accepting. Accepted and committed 99d123e (base 73e173c). 1002/1002 unit tests, lint/typecheck/check clean.
Task 4: reviewer (oc_msgpr13y_aea4768a, openai/gpt-5.6-luna --variant max), briefed specifically on the root-path fix history: Approved, no issues. Independently re-verified the overlap/childPrefix logic and the literal `/` regression test per the dispatch prompt's instructions, not taken on trust.
Task 4: complete (commits 73e173c..99d123e, review clean, no fix loop needed — the only defect was caught and fixed pre-review)
Task 5: implementer done (oc_msgq13dk_0bd9a2f0, minimax/MiniMax-M3), commit c0fc04e (base 2d8af9a). Dispatch supplied verified-real line numbers for 4 wiring points (initManagerMaps at 3344, the pre-existing killGateAndWait stub at 3443-3451 from Task 2, extractChangesetForTask: at 3438, the hasChanges branch at 4530) since the brief's own numbers had drifted after Tasks 3-4 landed — avoided the "implementer must locate it itself" risk flagged in this session's continuation prompt. Two lint-driven extractions (maybeStartCheckGate, wireCheckGateChildHandlers/markCheckGateFailedWithError) kept functions under eslint's complexity/length caps; confirmed behavior-preserving against the brief's inline form. 1007/1007 unit tests, lint/typecheck clean.
Task 5: reviewer (oc_msgqoz12_e4dec639, openai/gpt-5.6-luna --variant max), briefed specifically to re-verify the two externally-reviewed security points (kill-mechanism `detached: true` + process-group signal; sandbox parity via Task 4's `resolveReadOnlyProjectBinds` + `runtimeDirWritable: false`) rather than trust the implementer's "no deviation" claim: both confirmed correct. Needs fixes on 3 other findings — 1 Important (`appendBoundedOutput` capped by `String.length`/UTF-16 code units despite claiming byte-exactness; verified directly: `"🎉".repeat(1000).length` is 2000 but real UTF-8 byte length is 4000 — a chatty non-ASCII check command could grow to ~2x the intended 256KB heap cap), 2 Minor (`lastLines` trailing-`\n` off-by-one, dropping the last real line; stale sourcemap line counts). All three verified directly before entering the fix loop.
Task 5: fix round 1/5 (3 addressed, 0 open; oc_msgr5c8y_c344d0e8 resumed same implementer session, commit 4020701 on top of c0fc04e). Fix report claimed "4 pre-existing environment failures" in the full suite — verified this myself: 1009/1009 clean in the real worktree, confirming the 4 (multiplexed-daemon, caller-env-union, bwrap-auth, client.js symlink-guard tests) are artifacts of the nested-sandbox test environment a ferry runs test suites inside (XDG path leakage + read-only runtime-dir filesystem), not real regressions — same pattern independently flagged by Tasks 3 and 4's implementers for the symlink test specifically. Re-review (oc_msgrhmib_71722c2f, openai/gpt-5.6-luna --variant max): ADDRESSED, no new Critical/Important breakage, sourcemap counts (5796/203) match `wc -l` reality.
Task 5: complete (commits c0fc04e..4020701, review clean after fix round 1)
Task 6: implementer done (oc_msgrpmg1_9a0fddde, minimax/MiniMax-M3), commit 573b1a9 (base 87c0609), 12 files. Dispatch corrected 4 stale tasks.js line refs (validateAcceptable 2723, acceptTaskChangeset 5028, rejectTaskChangeset 5087, accept:/reject: factory bindings 3581/3586 — already threading killGateAndWait per Task 5). Implementer refactored the brief's inline form into 3 extra helpers (applyAcceptedChangeset, finalizeAcceptedChangeset, validateCheckGateAcceptable) + a BLOCKING_CHECK_STATUSES set — verified behavior-preserving directly against the brief (same guard order, same thrown messages, same checkOverride/checkStatus return shape). Implementer self-diagnosed the recurring "pre-existing environment failures" pattern for real this time: inherited TASKFERRY_*/XDG_*_HOME env vars causing a socket collision in the daemon-client tests, not a real regression — fixed by clearing those vars for its own test run, reaching a genuine 1018/1018. Verified myself with the same clean-env approach: 1018/1018, lint/typecheck clean.
Task 6: reviewer (oc_msgsya9x_0bb59cda, openai/gpt-5.6-luna --variant max), briefed to independently re-verify the refactor's behavior-preservation and 5 specific correctness points (async await ordering in both accept/reject, RPC detail round-trip via a real socket test, checkOverride on all 4 blocking statuses including "running", reject's unconditional-allow, the CLI "no check" warning's null/"none"-only trigger): Approved, no issues, no fix loop needed.
Task 6: complete (commits 87c0609..573b1a9, review clean)
Task 7: implementer done (oc_msgugn7v_dbbe088b, minimax/MiniMax-M3), commit 908571e (base e0633f1). Dispatch corrected stale tasks.js line refs from the brief (bootstrapManagerContext 3876, sweepOrphanedOverlays/sweepOrphanedPromptFiles bindings 3633-3634, sweepOverlayTmpRoot/sweepOverlayEntry 2511/2530). Implementer folded the brief's two review-fix notes (changesetStatus !== "pending" guard; best-effort orphan-kill via sendSignal before re-invoking startCheckGate) directly into markInterruptedGatesFor rather than as follow-up patches. 5 new tests in tasks.lifecycle.test.js (interrupted-on-load, settled-untouched, decided-task-untouched, live-overlay-auto-rerun, orphan-kill-before-rerun). 1023/1023 unit tests (clean env), lint/typecheck clean. accept hit the usual scratch-file conflict (Gotchas); resolved with the established recovery, ledger content verified unchanged by diff before appending.
Task 7: reviewer (oc_msgvauzg_31f2be0c, openai/gpt-5.6-luna --variant max): Needs fixes, root cause execution-error — 1 Minor: the orphan-kill-before-rerun test asserted kill and spawn each happened but not their relative order, so a regression spawning the new gate before killing the orphan would still pass. 0 Critical/Important; core implementation, guard, wiring, and JSDoc confirmed correct.
Task 7: fix round 1/5 (1 addressed, 0 open; oc_msgviuau_1b3f7bff resumed same implementer session, commit 5b7e7b8 on top of 908571e — dispatched with --no-overlay direct-to-worktree rather than the overlay/changeset flow, landed straight to git with no accept step). Test-only change: records kill/spawn calls into one shared ordered sequence and asserts killIdx < spawnIdx. Implementer verified the assertion actually catches a regression by temporarily flipping the production order and confirming the test failed, then reverting. 1023/1023 (clean env), lint/typecheck clean. Re-review (oc_msgvq4ev_a1deb27a, openai/gpt-5.6-luna --variant max): ADDRESSED, no new findings, confirmed test-only (no production files changed).
Task 7: complete (commits e0633f1..908571e..5b7e7b8, review clean after fix round 1)
Task 8: implementer done (oc_msgvuoka_158e4916, minimax/MiniMax-M3), commit fb27bec (base 218a318). Brief was largely self-contained (new files); confirmed resolveProjectConfigPath (project-config.js:26) and the setup/runSetupCommand daemon-bypass pattern the brief tells you to mirror (cli.js:16,35-36,48) before dispatching. Implementer caught and fixed a real bug in the brief's own literal runInit snippet: the no-TTY-with-detection branch would have written the generic fill-in template instead of the detected command commented out, failing the brief's own test assertion — fixed with renderConfig(detected).replace(/^check =/m, "# check ="). 1035/1035 unit tests, lint/typecheck clean.
Task 8: reviewer (oc_msgwcpd2_d209481f, openai/gpt-5.6-luna --variant max): Needs fixes, root cause mixed — 2 Important: (1) packageCheckCommand's try/catch swallowed both a readFileFn IO error and a JSON.parse error identically, violating this repo's fail-fast convention (a real read error should propagate, not fall through to "nothing detected"); (2) zero test coverage of the TTY confirmation branch (readline accept/reject). Independently verified the no-TTY-detected deviation from finding 1's session was correct before dispatching the fix.
Task 8: fix round 1/5 (2 addressed, 0 open; oc_msgwmbj7_2eaaf064 resumed same implementer session, commit 17eef90 on top of fb27bec, --no-overlay direct-to-worktree). Split packageCheckCommand's try/catch to wrap only JSON.parse, letting a real readFileFn throw propagate; injected createReadlineInterface the same way existsFn/writeFileFn/detect are already injected, added TTY-accept and TTY-reject tests asserting on actual written file content. incomplete: true (no closing Status line) but diff independently verified real and matching the claim. 1039/1039 (clean env), lint/typecheck clean. Re-review (oc_msgwwvqj_cdf1aac6, openai/gpt-5.6-luna --variant max): both original findings ADDRESSED. 1 new Minor: docs/sourcemap.md's init.test.js line count (138) stale against the fix's real 141 lines — fixed directly inline (commit 4e3bff0) rather than another dispatch round-trip, per this repo's dispatch-sizing convention for single-line mechanical doc fixes.
Task 8: complete (commits 218a318..fb27bec..17eef90..4e3bff0, review clean after fix round 1, 1 trivial doc-only minor fixed inline post-approval)
Task 9: implementer done (oc_msgx3hf8_8bf22d6c, minimax/MiniMax-M3), commit 537ed77 (base f4a70a2). Added .taskferry.toml (check = "npm run check"), grew package.json's check to append && npm test, documented the #292 TASKFERRY_CHILD leak and its existing env -u workaround in docs/security.md. Implementer's own npm run check hit the exact known env-leakage artifact on first pass (2 sandbox tests failing on inherited XDG_DATA_HOME), then a clean re-run with that also unset: 1039/1039.
Task 9: reviewer (oc_msgxe0jm_dea7feff, openai/gpt-5.6-luna --variant max): Approved, zero findings. Verified the .taskferry.toml content, package.json script ordering, and security.md accuracy directly; ran a clean-env npm run check itself (1039/1039). Could not independently check GitHub issue #292's text (gh-axi hit AUTH_REQUIRED in the sandbox) — noted honestly rather than guessed.
Task 9: complete (commits f4a70a2..537ed77, review clean, no fix loop needed)
