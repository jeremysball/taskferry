You are implementing Task 9 early as Task 7's test-harness prerequisite.

Read `/workspace/taskferry/.superpowers/sdd/task-9-brief.md` first; it is your requirements.

Tasks 1-6 are complete through `fa0e68e`. Add only `stdout` to `fakeChild()` and `defaultExecutor` passthrough to `makeManager()` in `src/tasks.test.js`, adapting to the existing helpers. Preserve every concurrent uncommitted summary-model/test/report change. Do not stash, reset, checkout, clean, revert, stage unrelated hunks, or bypass hooks.

Run focused tasks tests, lint/typecheck, and relevant unit verification. Commit only Task 9 hunks normally with the brief's Conventional Commit message.

Write `/workspace/taskferry/.superpowers/sdd/task-9-report.md` with exact changes, tests/results, staged hunks, self-review, and concerns. Return only status, commit, one-line test summary, concerns, and report path, ending with `Status: DONE|DONE_WITH_CONCERNS|BLOCKED|NEEDS_CONTEXT`.
