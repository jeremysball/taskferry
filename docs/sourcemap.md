# Sourcemap

A one-page orientation for anyone (human or agent) new to this codebase —
what each file does and where to look for something, not the full behavior
of any of it. For depth, follow the doc pointers in each section.

## Call chain, front to back

```
cli.js          entrypoint: parses process.argv, dispatches to a command,
                 prints TOON, sets process.exitCode
  -> args.js       flag/argument parsing and validation per command
  -> commands.js   translates parsed options into daemon RPC calls,
                    shapes the response (leanStatus/leanResult/...)
    -> client.js     connects to the daemon socket; auto-spawns the daemon
                      on first use if none is listening
      -> daemon.js     long-lived process: Unix-socket JSON-RPC server,
                        owns one TaskManager, dispatches event.subscribe
        -> tasks.js      the task lifecycle: dispatch/cancel/status/poll/
                          list/result/tail/summarize/advisor, state
                          persistence, watchdogs, queueing
        -> protocol.js   RPC envelope shape, PROTOCOL_VERSION, method list
        -> events.js     per-daemon event sequencing/emission for watch
        -> activity.js   cached narration-to-summary snapshots (the model
                          call behind --mode activity / watch --summaries)
        -> state-lock.js cross-process file lock; guards
                           daemon auto-start race
  -> output.js     lean field projection, TOON encode (non-TTY) / pretty.js
                    render (TTY), MCP-era hint migration
                    (taskferry_dispatch -> taskferry dispatch)
    -> pretty.js   shape-based "minimal accent" renderer for a real
                    terminal
opencode-plugin.js  native OpenCode plugin: calls client.js directly,
                     not through cli.js
```

`src/tasks.js` (6092 lines) is the largest file by a wide margin and does
the real work; everything above it is thin. If a bug report doesn't
obviously belong to args parsing or output formatting, start there.

## File-by-file

| File | Lines | Responsibility |
|---|---|---|
| `cli.js` | 215 | Entrypoint. Direct-execution guard (`fs.realpathSync(argv[1]) === import.meta.url`, symlink-safe) so it's importable without side effects. `usesWorkspaceRoot()` short-circuits to `false` whenever `options.all` is set (list/watch `--all`) so `normalizeCommandDirectory()` never overwrites the `directory: void 0` args.js already cleared with a resolved cwd root (taskferry#315). `setup` and `init` bypass the daemon client; `init` passes literal cwd to `runInit()` so worktree scaffolding is never redirected to the main checkout. |
| `args.js` | 400 | Per-command flag specs (`FLAGS` table: allow-list, boolean/value, coercer, migration hints), defaults (`DEFAULT_OPTIONS`, derived per-command from `FLAGS`' own allow-lists via `flagDefaultsFor()` so the two tables can't drift out of sync -- the handful of defaults `FLAGS` can't express, like the positional task id or `summary`'s `mode: "report"`, are layered on top), validation split into per-command validators (`validateResult`/`validateWait`/`validateWatch`/`validateDoctor`) called from `validateCommand`. Rejects retired MCP-era names (`poll`, `--task-id`, `--timeout_ms`) with a rename hint. `dispatch` exposes `--no-overlay`; `advisor` deliberately does not — overlay is mandatory for the advisor role per ADR 0001, so passing `taskferry advisor --no-overlay` errors with the standard "unknown flag" UsageError at parse time. `advisor`'s `--prompt` is optional (auto-attaches caller context when omitted) and accepts `--summarize-context` (boolean; off by default) which triggers commands.js's throwaway-model condensation of the auto-attached context. `result` exposes `--diff` (mutually exclusive with both `--fields` and `--full` — the latter was the pre-fix silent-drop regression where the if/else-if chain in commands.js picked `fields:["diff"]` and lost `--full`); `doctor` exposes `--stats` (boolean, off by default, mutually exclusive with `--full` at parse time); `accept`/`reject` are no-flag, positional-only commands except that `accept` accepts `--force` (boolean; Task 6 wires the actual gate-supervisor override behavior) and `dispatch`/`advisor` accept `--parent-task <id>` (stored under `parentTaskId`; raw-RPC equivalent of `task.dispatch`/`task.advisor`'s `parentTaskId` field -- the parent task's check-gate failure message can then echo this dispatch's id). `--all` is now shared by `list` and `watch` (`applyAllFlag()`, generalized from the old list-only `validateList()`): rejects combining with `--directory` (both) or `--task-id` (watch), otherwise clears `options.directory` to `void 0` so the daemon streams/lists every workspace (taskferry#315). |
| `command-specs.js` | 116 | Per-command `usage`/`description`/`options` (help-text strings only, keyed by command name) and `examples`, imported by `args.js`'s `helpText()`/`usageError()` for `--help` output and valid-flag hints. Purely descriptive: it carries no default values or parse-time behavior, so it can't be used to derive `args.js`'s `FLAGS`/`DEFAULT_OPTIONS` tables — those stay args.js's own single source of truth. |
| `commands.js` | 486 | Per-command dispatch table (`HANDLERS`) keyed by command name — `runCommand` resolves default deps and calls the matching handler, the only place that calls `client.request` for the per-command RPCs. The advisor-context helpers (`resolveAdvisorContextChars()`/`claudeTranscriptPath()`/`extractTranscriptText()`/`gatherAdvisorContext()`, plus the `ADVISOR_CANNED_PROMPT` constant) now live in `advisor-context.js` and are imported here for the advisor command to wire in. `summarizeContextText()` (module-scope; not exported) is the best-effort throwaway `task.dispatch`/`task.wait`/`task.result` condensation used when `advisor --summarize-context` is set — returns the input text unchanged on any failure so condensation can never break an otherwise-valid advisor call, and uses `env.TASKFERRY_ADVISOR_SUMMARIZER_MODEL` (fallback `opencode/mimo-v2.5-free`) so the summarizer is env-overridable. The `summary` command omits `env` from the RPC payload when `--mode activity` is set, since the activity path reads the cached task activity and spawns nothing — protocol.js rejects the combination for direct RPC callers as a validation error. `dispatch` forwards `noOverlay` and `parentTaskId`; `advisor` forwards `parentTaskId` (not `noOverlay` -- advisor's args.js spec excludes `--no-overlay`, so the field is unreachable from the CLI; protocol.js's `task.advisor` `hasOnly` set also rejects the key for raw RPC callers). `result --diff` selects `fields: ["diff"]` and overrides `--fields`; the `result --diff --full` combination is rejected at parse time by args.js (the field selection in the handler is now deterministic under that gate). `accept`/`reject` call `task.accept`/`task.reject` (Task 2: `accept` forwards `force: true` only when the CLI flag was actually set, not as a default) and surface `cleanupFailed` as a stderr warning so a leftover overlay isn't invisible until the daemon-restart sweep. `doctor --stats` skips the environment-check `Promise.allSettled` block entirely, calls `task.list` directly, and returns `computeDoctorStats()`'s output unwrapped (no `integrations`/`warnings`/`info`). The watch streaming — `client.subscribe` and the catch-up `task.status` RPC — lives in commands-stream.js (extracted from this file to drop max-lines below 400 while keeping the per-command handlers in their natural home). |
| `commands-stream.js` | 158 | The watch streaming code split out from commands.js: `streamTaskEvents()` (subscribe + buffered/ndjson flush + abort-aware settle, including the catch-up `task.status` RPC for subscriptions that complete after the task already settled), `watchCommand()` (resolves the watch directory from `--all` / `--directory` / `--task-id` / cwd-default and closes the client when the stream finishes), `subscribeSelector()` (picks `{all: true}` / `{directory}` / `{taskId}` for the `event.subscribe` call, in that precedence), and the shared `TERMINAL_STATUSES` set. `watch --all` sends `{all: true}` instead of a directory, so the daemon streams every workspace's events unfiltered (taskferry#315). Imported by commands.js's `watch` and `wait --summarize` handlers. |
| `commands-stream.test.js` | 336 | Split out of `commands.test.js` (sonarjs max-lines fixup, merge of #340 and #334): event printing/coloring/collapsing, `--task-id`/`--all` subscription selection, the already-settled catch-up path, and `--flush-interval` batching/abort-flush. Self-contained — its own tmp-dir/`fakeClient`/`fakeIo`/`setupWatch` helper copies, only imports `runCommand` from `./commands.js`. |
| `advisor-context.js` | 198 | Advisor's auto-attached-context machinery, extracted out of `commands.js`: `resolveAdvisorContextChars()` (env → config → 120000 default), `claudeTranscriptPath()` (Claude Code project-slug transcript path resolution), `extractTranscriptText()` (tail-N-code-points file read with a `UsageError` on unreadable transcripts, user/assistant-only extraction), `gatherAdvisorContext()` (the top-level combinator `commands.js` calls), and the `ADVISOR_CANNED_PROMPT` constant. |
| `advisor-context.test.js` | 156 | Tests for `advisor-context.js`'s exported functions above. |
| `client.js` | 446 | Daemon connection, auto-spawn-on-first-use, request/response correlation by id, `subscribe()` for events. `validError()` requires the daemon's full error envelope (`code`/`message`/`help`/`detail`); `buildRequestError()` (Task 6) prefers `error.detail` over its synthesized `message + help` body when `detail` is set and distinct from `message`, so a multi-line manager failure (Task 6's check-gate fix-forward message) survives the daemon→client hop instead of being collapsed to two short lines. |
| `daemon.js` | 503 | `net.createServer`, one socket per client, request dispatch loop (`invoke()` routes validated RPC methods to the task manager; `task.summary`/`task.advisor` forward the whole validated params object to `manager.dispatch`/`summarize`/`advisor`, so new fields ride through without a daemon.js change; `task.accept` forwards `params.force === true` as `{ force }` to `manager.accept(taskId, options)` — Task 6 wires the actual override behavior, the daemon's only job here is to pass the flag through; also routes validated `task.reject`), `event.subscribe` bookkeeping, stale-socket takeover logic (`prepareSocket`, with a `dev`/`ino`/`ctimeMs` identity check on the stale socket so an unlink-then-recreate inode reuse can't be mistaken for the still-live original — taskferry#(stale-socket race)), source-signature self-restart orchestration. `responseError(id, code, message, help)` (Task 6) threads `error.message` (or the raw `Error`'s `text` for non-ProtocolError throws) as the new `detail` arg to `protocol.js`'s `errorResponse`, so a multi-line manager failure (Task 6's check-gate fix-forward message) reaches the client verbatim. `startDaemon(options)` resolves its defaults via `resolveDaemonOptions()` (option-object merge, not inline defaults) and hands the per-client socket/connection layer off to the helpers in `daemon-server.js` (`createDaemonServer`, `deliverEvent`, `syncActivitySubscriptions`, `makeClose`, `makeMaybeRestart`), passing the `invoke`/`responseError` delegates and mutable collections in as deps. `resolveDaemonOptions()` also creates one `resolveWorkspaceRoot` per `startDaemon()` call (`paths.js`'s `createWorkspaceRootResolver()`, or an injected override for tests) and threads it into both `invoke()` (so `task.list`/`task.context`'s `filteredTaskDetails()`/`filteredList()` compare directories via `sameWorkspace()`, not raw `===`) and `deliverEvent()`, so a repo-root-scoped `list`/`context`/`watch` also matches a task dispatched into a linked worktree of the same repo (taskferry#315). |
| `daemon-server.js` | 241 | The daemon's connection layer, extracted from `daemon.js`: `createDaemonServer` (socket lifecycle, `cleanup`, the per-line request dispatch loop via `dispatchRequest`), `subscribeRequest`/`subscriptionDirectory()` (event.subscribe bookkeeping: `all: true` maps to the `ALL_DIRECTORIES` (`null`) sentinel, an explicit `directory` wins next, otherwise the daemon resolves it server-side from `taskId` — issue #59), `dispatchRequest` (parse, SERVER_BUSY gate, in-flight tracking, `maybeRestart` in `finally`), `deliverEvent`/`eventPayload` (event fan-out including `activityVariants` summary-variant selection; a subscription's `ALL_DIRECTORIES` directory always passes, otherwise it matches via the injected `resolveWorkspaceRootFn`/`sameWorkspace()` rather than a raw `===`, so a repo-root-scoped `watch` also receives events for a task dispatched into a linked worktree — taskferry#315), `syncActivitySubscriptions` (groups subscriptions by directory, including the `null` "all" bucket, for `tasks.js`'s `scheduleActivityFor()` to union in), `makeClose` (graceful shutdown: wraps `manager.close?.()` — which tears down `env-file.js`'s `watchEnvFile()` watcher, among other manager-owned resources — in its own try/catch so a throw there can't skip the shutdown broadcast/`server.close()`/socket unlink, leave `maybeRestart()`'s `restarting` latch stuck `true` forever, or reject `close()` out from under the top-level SIGTERM/SIGINT handler in `daemon.js`), and `makeMaybeRestart` (deferred-until-idle self-restart). Exports the `MAX_BUFFER_BYTES` constant. `dispatchRequest`/`createDaemonServer` receive `invoke`/`responseError` from daemon.js via deps (no circular import). |
| `daemon-client.test.js` | 451 | Split out of `daemon.test.js` (sonarjs max-lines fixup): "multiplexed daemon client" coverage — request correlation, subscriptions, and auto-start; boot-failure reporting and malformed messages. |
| `daemon-subscriptions.test.js` | 534 | Split out of `daemon.test.js` (sonarjs max-lines fixup): workspace-filtered `list`/`context` coverage and `event.subscribe` routing/teardown (taskId resolution, summary-variant selection, disconnect cleanup, slow-subscriber backpressure disconnect). Self-contained — its own `temporaryPaths`/`fakeManagerFactory`/`openPeer` helper copies and constant defs, only imports `startDaemon` from `./daemon.js`. Also `gitWorktreeFixture()` (a real `git init` + `git worktree add` pair) backing the "git-worktree-aware workspace matching" describe block (taskferry#315): root-scoped `list`/`context`/`event.subscribe` match a task in a linked worktree, two unrelated repos don't cross-match, and `event.subscribe({ all: true })` receives every directory's events. |
| `tasks.js` | 6373 | `createTaskManager()`: dispatch, cancel, accept/reject, status, poll (`wait`'s RPC target), list, result (including cached diff/diffStat projection -- diffStat routed through `git apply --numstat` via the runOverlayCommandFn delegate so the same parser handles both git and non-git changesets, with a zero-stat fallback on any non-zero git exit status -- `git apply --numstat` exits 0 on success and any non-zero status indicates a failed parse, so partial stdout from a failed invocation is never read; the zero fallback is the documented "stat is uncomputable" path), tail, summarize, advisor, conditional changeset-field summary exposure (also surfaces `headDriftFrom`/`headDriftTo`/`headDriftRecovered` audit fields when settlement stamped a stale-base drift), state persistence (`tasks.json`), settlement-time `finalStatus` parsing from the worker's closing `Status:` marker (via `evaluateOutputCompleteness()` + `STATUS_MARKER_RE`, reusing `result()`'s own `parseTaskLog()`/`shapeNarration()` NDJSON parser via the shared `readFinalMessageState()` helper rather than a second one), `attemptCrashRecovery()` (a `crashed` exit whose log genuinely reached a `step_finish reason "stop"` with real text is reclassified to `done` at settlement -- covers a transient mid-run error the worker recovered from, and a `no_output_timeout_stalled` kill after a real stop event, but never a cancelled task or a `no_output_timeout_dead_spawn` eventless crash; `failureReason`/`failureDetail` are left in place as a record rather than cleared, and its parsed-message result is threaded into `evaluateOutputCompleteness()` on the same settlement to avoid re-reading the log), stale-base drift settlement at task-finish time (`extractChangesetForTaskRecord` -> `settleChangesetAfterExtraction` walks the precedence chain -- advisor -> recovered:false conflict auto-rejects and releases the overlay, recovered:null inconclusive settles pending with the infra failure recorded via `changesetError`, recovered:true and no-drift fall through to the existing `hasChanges` / no-changes branches), `--class` task-classification tag (freeform string accepted via `protocol.js`'s `isNonEmptyString`; persisted on the task record; surfaced in `result --fields class` and `status`; propagated to advisor dispatches), the no-output watchdog (split `failureReason`: `no_output_timeout_dead_spawn` when the task never wrote a parseable event, `no_output_timeout_stalled` when it did and then went silent past the escalated post-output budget; `consumeWatchdogLogChunk()`'s activity clock resets on *any* new log bytes, not just a complete parseable line -- a parseable line can't appear without the log growing first, so raw growth is a strict superset signal and the old parseable-line-only gate was needlessly narrower; `hasParseableLine` still separately gates the pre/post-output budget-escalation latch, since which timeout duration applies is a different question from whether the clock resets at all; `consumeWatchdogLogChunk()`'s single-tick read is capped at `WATCHDOG_CHUNK_READ_BYTES` (= `TAIL_READ_BYTES`, 1 MiB) rather than one `Buffer.alloc` for the whole tick's growth, with `state.bytesRead` advancing by the actual bytes read so an oversized burst is picked up over however many subsequent ticks it takes; `watchdogTick()`'s log-read catch only swallows `ENOENT` (rotated/not-yet-created log, retried next tick) and fails the task explicitly with `failureReason: "watchdog_log_read_error"` on any other read error instead of silently freezing the activity clock; a separate absolute `preOutputMaxMs` ceiling (`TASKFERRY_PRE_OUTPUT_MAX_MS`, default 4x `noOutputTimeoutMs`), measured from `state.armedAtMs` rather than `lastActivityMs` so continued raw growth can't push it back, kills a task that rides the any-growth reset forever without ever producing a single parseable line -- stops applying once `outputSeen` flips true), queueing/concurrency caps, caller-env union sanitization (three-layer merge -- `envFileVars` loaded once from `envFilePath` at `createTaskManager()` construction (and kept live thereafter by `env-file.js`'s `watchEnvFile()`, parent-dir-watched with a short debounce so a mid-run secret rotation takes effect on the next spawned child without a daemon restart -- a torn read caught mid-rewrite is rejected by pre/post `mtime`/`size` comparison, an `onReload` exception is routed through `onError` instead of escaping the debounced `setTimeout`, and the watcher's stderr-write of the warning itself is guarded against EPIPE so a closed/rotated stderr doesn't kill the watcher) -- the live watch is established by the module-scope `startEnvFileWatch()` helper rather than the original factory's inline `if (envFilePath) { ... }`, with `envFileWatcher`/`envFileVars` carried through the manager's `state` object instead of as closure `let`s; below the daemon's own ambient `process.env`, below the caller's forwarded env) and denylist enforcement, filesystem `sandboxDenylist` merged with `sandbox.js`'s fixed default deny-list at every bwrap call site (dispatch launch, changeset extraction, changeset apply), in-`sanitizedEnvironment` env-key validation (mirrors the RPC-level `isEnvironment` check so programmatic callers can't bypass it), dispatch-time env snapshot (clone of caller env, the b45de81 defence against caller mutations between queue time and spawn -- pinned by a verification-gate test, see BLOCKED in commit history), summary-path env deferred to spawn time (so the spawned summary child sees the daemon's current process.env, matching dispatch's deferred `dispatchEnvironment()`), per-caller-env models cache (`modelsCache` keyed by a fingerprint of model-relevant caller vars (`*_API_KEY`, `*_BASE_URL`, `OPENCODE_CONFIG*`, `OPENCODE_AUTH_CONTENT`, `OPENCODE_MODELS_PATH`/`_URL`, `PI_CODING_AGENT_DIR`), not time alone, so callers with different credentials or catalog/endpoint overrides don't read each other's listings, plus an in-flight `Map` to coalesce concurrent populates for the same key), daemon-startup orphan overlay sweep across both the live tmp root and every creation-time tmp root persisted on a task, centralized overlay release/cleanup-failure tracking, plus overlay-enabled wiring (`overlayEnabled`/`checkOverlaySupportFn`/`overlayTmpRoot` (defaults to `paths.js`'s `resolveOverlayTmpRoot({ env: process.env, runtimeDir })`, scoped under `runtimeDir` rather than plain `os.tmpdir()` -- taskferry#286; exposed back out on the returned manager as `paths.OVERLAY_TMP_ROOT`)/`rmOverlayTreeFn` options, `requireOverlaySupport()` capability check with a 60s TTL on negative probe results so a transient bwrap failure (PATH mid-update, version-too-old mid-upgrade) can self-heal without a daemon restart -- a positive result is cached forever since no transient issue would flip it back, `noOverlay`/`role` dispatch params, `role`/`changesetStatus`/`diffPath`/`overlayDirs` (including creation-time `tmpRoot`)/`preDispatchHead`/`changesetError` task-record fields), spawn-path overlay construction (CoW overlay on `directory` when `overlayEnabled && checkOverlaySupportFn().supported`, with the per-path git-common-dir sub-overlays persisted on `task.overlayDirs.rwBinds` for extraction to re-mount verbatim -- review finding #1, and writable git-common-dir FILES like `packed-refs` -- which overlayfs can't mount, being directory-only -- routed instead through `subFilePaths()`/`overlayRwFileBinds`: a scratch copy bound rw onto the host path, persisted on `task.overlayDirs.rwFileBinds` for extraction to re-mount the same way), `preDispatchHead` resolved through the injected `runOverlayCommandFn` delegate (not a direct subprocess call) so the same test fakes that cover settlement-time extraction cover the dispatch-time git probe too, the `child.on("error")` (spawn-failure) path AND the synchronous try/catch around the `spawnFn` / overlay-setup / pre-dispatch-HEAD block both run the same `extractChangesetForTask()` the exit path does -- so a task whose overlay was already created before spawn fails (whether asynchronously via `child.emit("error", ...)` or synchronously via a throwing `spawnFn` / `resolvePreDispatchHead`) is never stranded with no extraction and no cleanup (final-review fix: the sync catch was originally missing the extract call, leaving overlayDirs set + changesetStatus === "pending"; the startup sweep deliberately skips pending owners so the overlay sat on the tmpfs indefinitely), advisor-role spawn isolation via `runtimeDirWritable: false` so the daemon's Unix socket inside runtimeDir is unreachable from the sandbox (review finding #6) -- advisor keeps `shareNet` at its default (network shared), since `--unshare-net` blocks all outbound network including the model-provider API calls the advisor role itself depends on, not just the daemon socket (fixed after shipping unshare-net'd advisors that could never reach a provider), advisor fail-closed when overlay-gating can't be established -- at dispatch-launch time a sandbox that is force-disabled (`--no-sandbox`/`TASKFERRY_DISABLE_SANDBOX=1`) or unsupported on the platform crashes with a `spawnError` rather than running unguarded, and inside the sandbox block an advisor with overlay globally disabled crashes likewise (review finding #5), accept() verifies the recorded diff file at `task.diffPath` still exists on disk before handing it to git apply (review finding #1 -- a partial stateDir cleanup or a tampered tasks.json would otherwise surface as a misleading `git apply` "can't open patch" message), and accept()/reject() persist `changesetStatus` to disk before the overlay cleanup so a crash between apply and cleanup can't leave the task reading as "pending" after a restart when the patch was already applied (review finding #4 -- the pre-fix order was persist-after-cleanup, which narrowed the window to a double-apply risk on retry), and on successful cleanup each path persists a second time so the durable task record reflects the cleared `overlayDirs` and doesn't keep claiming an overlay still exists for an overlay that was just removed (review followup #1 -- on cleanup failure the second persist is skipped so the persisted `overlayDirs` survives for the daemon-startup sweep to retry), and settlement-time boot-failure surfacing (`extractBootFailureDetail`/`logHasAnyEvent`: an eventless non-zero exit gets a `boot_failure` bucket whose `failureDetail` is the last captured `Error:` line, plus a `tail()` fallback that shows the raw capture for eventless crashed tasks instead of `none observed yet`). Also: `cancel()` and the env-builder helpers (`sanitizedEnvironment`/`dispatchEnvironment`/`summaryEnvironment`/`releaseOverlay`/`extractChangesetForTask`) were extracted to module scope during the sonarjs lint-fixup — the env builders resolve their own sibling helpers via local closures rather than `ctx.env.X`, since `ctx.env` isn't populated on the partial context object passed to their own builder function. Also: `launchQueuedTasks()`'s drain loop enforces a global minimum spacing (`lowerdirStaggerMs`/`TASKFERRY_LOWERDIR_STAGGER_MS`, default 3000ms) between *every* dispatch/advisor launch, regardless of directory — a workaround for a bwrap overlay-mount race (taskferry#318, `Can't make overlay mount ... Device or resource busy`) confirmed on real hosts both within one worktree and across different worktrees of one repo sharing a git-common-dir; deliberately global rather than per-directory (see `.superpowers/.completed/specs/2026-08-03-lowerdir-launch-stagger-design.md` for why a per-directory gate was rejected). `extractChangesetForTask()`'s catch block also reclassifies a caught extraction error as `failureReason: "overlay_mount_busy"` (overwriting whatever the exit-path classifier guessed, e.g. `no_output_timeout_dead_spawn`) when the caught message matches bwrap's exact overlay-mount-busy wording — the stagger reduces the race but can't guarantee it never recurs, so a confirmed cause should never be buried behind a generic timeout bucket. `dispatchTask()`'s prompt augmentation (Task 3: `.taskferry.toml` `check` injection): when the project declares a `check` command in `.taskferry.toml`, dispatch prompts get the `verificationPromptBlock()` block appended (advisors explicitly do NOT — the gate is meaningless on an advisory role); both the persisted task promptPreview and the launched argv reflect the augmented prompt, and the bare `prompt` variable in `dispatchTask`'s body is replaced only at the two call sites (buildDispatchTask + queueDispatchLaunch). `applyProjectConfigReadOnlyBinds()` (Task 4: `.taskferry.toml` `read_only_paths` → validated bwrap ro-binds): called from `buildBwrapBinds`, runs `resolveReadOnlyProjectBinds()` (shared with Task 5's gate for sandbox parity) against the protected mount set (`denyList` + `stateDir` + `runtimeDir` + `launchDirectory`) -- both call sites pass the *unfiltered* `fullDenyList` here, not the existence-filtered `denyList` used for the actual bwrap mount args, since an overlap with e.g. `~/.ssh` is unsafe whether or not that path exists on this particular host yet; a fresh CI runner with none of the deny-list dotfiles present would otherwise let `read_only_paths = ["$HOME"]` sail through unrejected, appends the safe pairs to `extraRoBinds`, and writes `task.projectConfigWarning` for any entry that's missing-on-host or overlaps a protected mount -- including the literal `"/"` (the `childPrefix` helper in the resolver returns `"/"` unchanged when `base === path.sep`, so `read_only_paths = ["/"]` is correctly classified as an ancestor of every protected path and rejected before reaching the bwrap argv). Task 5's gate writes the same field for parse errors (overwrite-not-append per the brief; the later writer in a task's lifecycle wins). `startCheckGate()` (Task 5: the `.taskferry.toml` `check` runner): fires from `extractChangesetForTaskRecord`'s `hasChanges` branch (gated on `isGitTarget` -- a non-git / overlay-only accept rsyncs the overlay onto the real directory, so anything the gate wrote into the overlay's upper would land on the real tree, exactly the contamination the gate is supposed to prevent); spawns the project's declared check command inside the SAME bwrap overlay mount the worker ran with (`task.overlayDirs` -- `upperDir`/`workDir`/`rwBinds`/`rwFileBinds`), so gate side effects (test caches, build artifacts) land in the overlay's upper, never on the real directory. Fire-and-forget and fully async (`ctx.spawnFn`, NOT the synchronous `runOverlayCommandFn` extraction uses -- a check can run up to `checkTimeoutSeconds` (default 900s) and must never block the daemon's event loop). Sandbox parity with the worker: same `denyList`, same `readOnlyBinds` (reuses `resolveReadOnlyProjectBinds` from Task 4), same overlay mount; `runtimeDirWritable: false` (security review finding -- `buildBwrapArgs` defaults it true, which would hand a worker-controlled check a writable bind onto the daemon's Unix socket so the gate could call `taskferry accept --force` on its own task). Gate spawned `detached: true` so `sendSignalToProcess`'s group-kill (the existing mechanism workers already use) reaches the sandboxed workload instead of just the bwrap monitor -- empirically required after an earlier plan draft assumed bwrap's `--unshare-pid` namespace teardown alone kills the workload, which was tested and disproven (7/7 orphan trials). Two new helpers: `lastLines(text, n = 40)` for the `checkOutputTail` tail-cap contract, and `appendBoundedOutput(tail, chunk)` which trims the combined stdout/stderr buffer from the front when it exceeds `CHECK_GATE_OUTPUT_CAP_BYTES` (256 KiB) so a chatty test suite doesn't grow the daemon's heap unbounded for up to `checkTimeoutSeconds`. Each in-flight gate's `ChildProcess` is tracked on `ctx.maps.gateChildren` (a new Map next to `tasks`/`waiters`/`advisorSessions`) so `killGateAndWait` (Task 6's accept/reject supervisor, replacing the build-time stub `killGateAndWait: async (_taskId) => undefined` left in place by Task 2) can find the same process object and await its actual exit -- sending a signal and proceeding immediately is a race, not a handshake: the overlay's upper can be `chmod -R`/`rm -rf`'d out from under a still-running gate. The gate's exit/error handlers also refuse to overwrite a `task.changesetStatus !== "pending"` record (Task 6 will move `changesetStatus` off "pending" via the accept/reject kill handshake). Wired into `buildManagerEnvHelpers`'s returned object as a genuine top-level `ctx.env.startCheckGate` key (not a closure buried in another function's argument object) so Task 7's restart-recovery sweep can call the exact same binding directly without going through `extractChangesetForTaskRecord`. Constants near `DEFAULT_CANCEL_GRACE_MS`: `CHECK_GATE_KILL_GRACE_MS = 5000` (SIGTERM->SIGKILL grace, matching `cancel()`'s default) and `CHECK_GATE_OUTPUT_CAP_BYTES = 256 * 1024`. `markCheckGateFailedWithError()` (sync-spawn-throw + async-`error` shared sink) and `wireCheckGateChildHandlers()` (stdout/stderr/exit/error wiring + timeout escalation + settle closure) are extracted from `startCheckGate` to keep the parent's line/complexity counts under the sonarjs caps. Task 7's daemon-restart recovery: `markInterruptedGatesFor()` (wired as `ctx.helpers.markInterruptedGates`) runs at boot, right after `sweepOrphanedOverlays()`; reclassifies any task stuck in `checkStatus: "running"` to `"interrupted"` (so Task 6's accept-gate supervisor keeps refusing it without `--force`, and the failure message renders the re-run path), with a `changesetStatus !== "pending"` guard so a task that was already force-accepted/rejected while its gate was "running" is left alone (a kill-handshake-raced decision must not be flipped to "interrupted" on every future restart), and a best-effort process-group SIGTERM against the persisted `task.checkGatePid` (the orphan from the previous daemon incarnation, since the gate was spawned `detached: true` and that pid IS the process-group leader) before auto-re-invoking `startCheckGate` if the task's overlay is still live, per the design's "the gate is re-runnable" promise. `parseProviderLimitsEnv()` (provider concurrency limits, taskferry#235): parses `TASKFERRY_PROVIDER_LIMITS`'s `provider:maxConcurrentTasks[:maxDispatchesPerWindow]` comma-separated grammar into a `Map`, defaulting an omitted third field to `Infinity` (unbounded dispatch rate) and throwing a two-line `error:`/`help:` message on any malformed entry rather than silently skipping it — a malformed limit is a startup-time config error, matching `config.js`'s posture. Per-entry parsing lives in the module-scope `parseProviderLimitEntry()` helper purely to keep the exported function under the sonarjs cyclomatic-complexity ceiling of 10. Launch scheduling is keyed per-provider: `providerOf()` derives a provider key from `task.model`'s `provider/model` prefix, and `providerQueues: Map<string, ProviderQueue>` holds one `{launchQueue, launchTimes, runningCount}` bucket per provider (replacing the single global `launchQueue`), round-robined by `drainLaunchQueue()` from a rotating `launchScheduler.cursor` so a provider parked at its own cap doesn't block another provider's queued work behind it — the global `launchTimes`/`runningCount` ceiling is retained and still binds across all providers, and a provider with no `providerLimits` entry falls back to the `UNLIMITED_PROVIDER` sentinel (see `docs/config.md`'s `providerLimits` field). `drainLaunchQueue()` delegates to `globalLaunchBlocked()`/`launchOneRoundRobin()`/`dropStaleQueueHead()`/`providerCanLaunch()`, and `scheduleNextLaunch()` to `soonestProviderDelay()`/`providerQueueDelay()`, purely to stay under the same sonarjs complexity ceilings. Gotcha: `providerQueueDelay()` must return the `CONCURRENCY_POLL_MS` (250ms) poll for a provider at its own *concurrency* cap, not just a rate-window deadline — a concurrency cap clears only when a running task exits, which fires no scheduler event, so omitting that term makes `scheduleNextLaunch()` re-arm on a 1ms timer forever and the process never goes idle. |
| `tasks.test-helpers.js` | 283 | Shared test scaffolding for the per-area `src/tasks*.test.js` files: every `const` fixture literal the test suite used to share at module scope (model names, error strings, env-var prefixes, temp-dir names), plus `fakeChild()` (an `EventEmitter` with the `pid`/`unref()` surface `dispatch()` touches) and `baseTask()` (a `done`-status task factory for `tasksFixture` overrides). The `makeManager()` fixture builder is here too -- extracted out of the old monolithic `src/tasks.test.js`, its option-plumbing is split into `makeTempDirs()` (the per-test tmp state/cache/overlay roots), `seedTestFixtures()` (writing `tasks.json` + `logs/<name>` before the manager loads them), and `buildManagerOptions()` (the per-test `createTaskManager` option object, with a `passthroughIfSet(inputKey, outputKey, transform)` helper that collapses the 20-line `...(x != null ? { x } : {})` chain -- and its cognitive complexity dropped from 27 to under 15 in the move). `sandboxEnabled`/`overlayEnabled` default to `false` in `buildManagerOptions` (not `undefined`) so they override the manager's own env-driven defaults; if a test wants the manager's default for either it has to pass the value explicitly. `mkdtempTracked(prefix)` and `trackManager(manager)` register a temp dir / a manager instance respectively in module-level arrays that a single shared `after()` hook drains at file teardown: every tracked manager's `flushPersist()` runs first (closing the ~250ms debounced-persist race against a manager whose stateDir is about to disappear), then every tracked dir is `rmSync`'d -- this is the fix for the test-suite's own `/tmp` leak (taskferry inode exhaustion incident), not a taskferry-overlay leak. `makeManager()` auto-wraps its manager in `trackManager()`; call sites that build a manager via a raw `createTaskManager()` call instead (bypassing `makeManager()`) must wrap it explicitly. The helpers file is intentionally NOT a `*.test.js` (so `node --test` skips it) and is not covered by the test-file ignore pattern in `eslint.config.mjs` -- so its `makeManager()` and helpers stay subject to the regular (non-test) ESLint complexity/length rules. |
| `tasks.parse.test.js` | 74 | `parseEnvDenylist()`/`parseSandboxDenylist()`: the two small argument parsers exported from `tasks.js`, with a couple of tests each (empty/undefined input, comma-split+trim+drop). Plus `parseProviderLimitsEnv()`: the `TASKFERRY_PROVIDER_LIMITS` grammar, covering the omitted-third-field `Infinity` default, multi-entry trimming, and each malformed-entry throw. |
| `tasks.persist.test.js` | 202 | `persistTask()` durability across concurrent manager instances (each one's task record survives the other's write; malformed `tasks.json` surfaces as a structured error), `isOutsideDirectory()` (genuinely-sibling / nested / `..`-prefix / parent path), `executorId on persisted tasks` (legacy records default to `opencode`; persisted `pi` survives), `dispatch() executor selection` (named executor resolves + stamps `task.executorId`; defaults to `pi`; unknown name throws), `unknown task id` (shared error path across `status()`/`cancel()`/`result()`/`poll()`), `taskDirectory()` (event.subscribe directory lookup), `status() log activity` (zero bytes / empty file / no event / at least one event / stderr noise prefix). |
| `tasks.dispatch.test.js` | 935 | `dispatch()` lifecycle (argv shape, session-id inheritance of model + executor + custom-executor reuse, prompt preview / promptTotalChars hint, directory symlink resolution, clean/non-zero/signal exit settlement, child.on('error') async spawn-failure cleanup, advisor-spawn auto-reject on spawn failure, synchronous spawn-throw cleanup, 247 lines split across two describes), `dispatch() input validation` (missing / non-string / unknown executor / relative / nonexistent dir), `dispatch() role/changeset fields` (no-overlay omits `role`+`changesetStatus`; advisor stamps `role: "advisor"`), `dispatch() with a prompt over the argv-safe size` (issue #78: E2BIG spawn-failure path -- inline pass under threshold, scratch file + `-f` attachment above threshold with mode `0o600`, scratch cleanup on settle / on child error), `dispatch queue` (FIFO at `maxDispatchesPerWindow`; queued cancel; queued poll), `lowerdir launch stagger` (taskferry#318: two/three+ queued launches spaced >= `lowerdirStaggerMs` apart; `TASKFERRY_LOWERDIR_STAGGER_MS=0` disables the gate), `active-task concurrency cap` (independent of launch-rate window) + regressions (double-settle `error`+`exit` doesn't over-promote; persistence-failure-after-spawn kills the child and releases the slot), `config file precedence` for `maxConcurrentTasks` (env wins over config; config used when env unset; built-in default when both unset), `dispatch() class tag persistence and summary surfacing` (class tag persisted on dispatch and surfaced in status summary; absent class omitted from summary), `dispatch() prompt augmentation from .taskferry.toml` (Task 3: augments a `role: "dispatch"` prompt with the `verificationPromptBlock()` whenever `.taskferry.toml` declares a `check`; never augments advisor prompts; leaves the prompt alone when there's no `.taskferry.toml`). |
| `tasks.sandbox.test.js` | 1039 | `bwrap sandboxing`, split into 10 sub-describes by topic so no describe callback exceeds the 200-line cap: dispatch argv shape + gitdir scoping (worktree private gitdir bound; redundant bind skipped when git-common-dir is inside `directory`; whole-common-dir fallback for submodule layouts / when gitDir resolution fails / when gitDir is non-standard; the taskferry#224 regression that scopes the bind to worktree's own admin + shared objects/refs only, never the main checkout's private HEAD/index/config), allowedDirs and denylist (manager-level default; per-dispatch override; missing entry skipped; deny-list missing-on-disk dropped; configured `sandboxDenylist` tmpfs-masked alongside the fixed defaults), opencode auth + data home (`XDG_DATA_HOME` rewrites to sandboxed cache; `auth.json` ro-bind when the real file exists; omitted when it doesn't; sandboxing-disabled leaves `XDG_DATA_HOME` untouched), platform + availability gating (--no-sandbox falls through; `sandboxEnabled: false` falls through; non-Linux falls through; missing bwrap crashes with `spawnError`; bwrap availability cached for the process lifetime), prompt + summary launches under bwrap (oversized-prompt PROMPT_DIR ro-bind; summary spawn's SUMMARY_DIR bind), overlay mount + probe gating (mounts when enabled + supported; pre-dispatch HEAD probe through `runOverlayCommandFn`; plain-bind fallback with warning when disabled; spawnError on overlay-required-but-unsupported; 60s negative TTL self-heal; positive result cached forever), overlay rwBinds + shareNet (git-common-dir sub-overlays instead of plain binds; share-net stays true for both dispatch and advisor), packed-refs file binds (regression: overlayfs mounts are directory-only, so a `packed-refs` file gets a scratch-copy rw bind via `overlayRwFileBinds` rather than a directory overlay; fresh repo with no packed-refs gets no file bind), advisor guardrails (runtimeDir read-only for advisor; advisor crashes instead of unguarded when overlay is disabled / sandbox is force-disabled / platform cannot sandbox), project-config read_only_paths (Task 4: `.taskferry.toml` `read_only_paths` become validated bwrap ro-binds; missing-on-host entry skipped + warned; homeDir ancestor entry rejected by the protected-mount check + warned; `read_only_paths = ["/"]` entry also rejected -- regression for the canonical root-shadow exploit where `"/" + path.sep` would otherwise become `"//"` and slip through the overlap check; the `/` test was added in a followup fix commit so the original feat commit doesn't show this test passing before its own fix). |
| `tasks.changeset.test.js` | 950 | `changeset extraction at settlement` (extracted diff + pending; advisor auto-reject + cleanup; persisted git-common-dir sub-overlays re-mounted during extraction; zero-change auto-accept; cancelled task still extracts; extraction error + recovery; `changeset extraction at settlement: stale-base drift settlement (taskferry#261)` (recovered drift settles pending with audit fields; unrecovered conflicting drift auto-rejects and releases the overlay; inconclusive probe settles pending with the infra failure recorded -- never silently assumed clean or auto-rejected); taskferry#318 `overlay_mount_busy` reclassification of a real bwrap overlay-busy extraction error over the watchdog's generic `no_output_timeout_dead_spawn` guess, and an unrelated extraction error left untouched), `boot-time sweep of orphaned prompt scratch files in PROMPT_DIR` (orphan-only / tracked-terminal / crashed-as-unknown / unrelated-files-ignored / mixed-orphan-and-tracked / empty-dir no-op / every-status reloads-as-unknown / first-boot creates PROMPT_DIR / mixed-orphan-and-tracked-terminal directory), `sweepOrphanedOverlays()` (unknown-task-id overlay removed; legacy-record tmpRoot migration to the pre-upgrade `os.tmpdir()` default; pending-changeset overlay NOT swept; empty tmpRoot no-op; resolved task overlay under its recorded non-live tmpRoot with `overlayDirs` cleared; taskferry#286: two managers with distinct runtimeDirs never collide on the same overlay namespace). `accept() check-gate gating` (Task 6: refuses a still-running gate; refuses a failed gate with the multi-line `buildCheckGateFailureMessage()` content including command/exit/timeout/interrupted/output-tail and `--session-id`/`--directory` resume hint; `--force` on a failed gate succeeds and stamps `checkOverride: true`; passed/`"none"` gate needs no `--force`). `reject() check-gate gating` (Task 6: reject always allowed regardless of checkStatus; reject while the gate is still running kills the gate and waits via `killGateAndWait()` before overlay release; a gate that settles after a reject is a no-op). |
| `tasks.watchdog.test.js` | 843 | `output-completeness check at settlement time` (issue #35) split into 3 sub-describes so the 219-line original fits under the 200-line describe cap: `task.activity` events carry `originSessionId`; `scheduleActivity` failure marker when the summary model call fails; clean done with a final message / empty final message / whitespace-only final message; `--require-final-marker` with matching / non-matching / combined-with-empty-message; crashed and cancelled tasks not relabeled; `finalMarker` regex validation; `originSessionId` storage; daemon-restart durability of `incomplete` + `finalMarker`. `finalStatus: parsed closing Status: marker at settlement` (DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT parsed from the final message; absent marker leaves field unset; independent of `--require-final-marker`; daemon-restart durability via tasks.json; `summarize()` surfacing of non-null `finalStatus`). The no-output watchdog coverage is split across 4 sub-describes to stay under the 200-line describe cap: `no-output watchdog` (running child with no parseable log event past the deadline is SIGTERM'd with `failureReason: "no_output_timeout_dead_spawn"`; `--fields failureDetail` value; UnknownError fallback for unmatched structured errors; `--fields` validation error message includes `failureDetail`; running child writing parseable events is left alone; repeated non-JSON output resets the watchdog since any log growth counts as activity); `no-output watchdog: absolute pre-output ceiling` (the ceiling fires despite continued non-JSON noise resetting the activity clock; does not apply once a parseable line has landed); `no-output watchdog: escalation and latch` (silence with zero log growth still fires the watchdog even after earlier non-JSON noise; silence-after-early-output is eventually stopped -- the GLM-5.2 review finding; pre/post-output budget escalation latch; first-tick-sees-existing-JSON latch flip); `no-output watchdog: read errors and chunk caps` (a persistent non-ENOENT log-read error fails the task explicitly instead of freezing the activity clock; a single tick's read is capped rather than one unbounded allocation, via `fs.readSync`/`fs.openSync` mocks). |
| `tasks.failure.test.js` | 1018 | `provider-failure classification` (rate_limit / payment / authentication buckets; 500-char truncation; SIGTERM'd child exiting 0 still lands on crashed; ordinary crash text is `pi_boot_failure` not a provider bucket; type:"text" mentioning rate limits isn't misclassified; insufficient_quota vs rate-limit language; bare quota's fallback bucket; 401 in plain English; `status_code: 401` without the word "unauthorized"; `no_output_timeout_dead_spawn` failureDetail; first-classification wins across watchdog ticks), `trailing provider-error events that land after the last watcher poll` (issue #81: classified even with no watcher tick; classified even when child traps exit 0; watcher-classified reason wins; clean exit reuses watcher's offset; settlement reads only the trailing delta via the injected delegate; clean exit with a watched-but-not-yet-classified error doesn't invent a reason; provider error split across carry and new bytes is still classified; classified when log no longer exists; classified when log shrank past watcher's offset), `bucketFor()` (Task 7 review fix: opencode buckets stay unprefixed; every other prefix gets underscore-joined), `classifyProviderFailure() honors the binding compatibility contract` (opencode named buckets unprefixed; pi named buckets prefixed; unknown structured events keep the executor prefix for both), `provider-failure classification is task-aware via task.executorId` (pi executor task with plain `NO_API_KEY_FOUND` settles with `pi_authentication_failed`), `boot-failure surfacing` (raw capture becomes `boot_failure` with the fatal `Error:` line; pi executor uses `pi_boot_failure`; non-Error last line; crash-after-real-events leaves `failureReason` null; exit 0 with raw text not classified; oversized event line blocks boot classification; oversized raw line yields no garbage detail; oversized noise before a real Error still surfaces the real one; unparseable brace-starting stderr is evidence; signal-killed eventless not classified; watcher-set failureReason survives eventless non-zero exit), `crash recovery: a genuine step_finish stop overrides a crashed status` (a transient mid-run error followed by a real final answer recovers `crashed` to `done`, keeping `failureReason` as a record; the fallback last-message rule alone doesn't count as recovery; a cancelled task is never reinterpreted; a `no_output_timeout_stalled` kill after a genuine stop still recovers). |
| `tasks.lifecycle.test.js` | 1215 | `cancel()` (SIGTERM to negative pid + SIGKILL escalation after `graceMs`; no SIGKILL if task already exited; stops the watchdog so cancellation can't add a failureReason; replaces existing escalation timer; signals + disables watchdog even when persistence fails; falls back to plain pid if `-pid` raises ESRCH; no-op note for non-running; persisted `running` task reloads as `unknown`; persisted queued task reloads as `unknown`). `accept()/reject()` (apply + accepted + cleanup; apply failure leaves pending + no cleanup; reject discards + cleans; reject cleans via recorded tmpRoot after live tmpRoot changes; advisor task throws on accept; no pending changeset throws; extraction-failed accepts preserve the overlay; missing diff file errors usefully; non-git target whose overlay vanished errors; failed cleanup surfaces `cleanupFailed`; persist-then-cleanup-then-persist order: status and `overlayDirs` are both durable). `accept(): overlaySleepFn threading (taskferry#328)` (its own standalone `describe` block, split out to keep `accept()/reject()`'s own body under the sonarjs function-length cap -- a non-git accept whose apply hits the overlay-mount-busy retry proves `overlaySleepFn` reaches `applyNonGitChangeset` via `acceptTaskChangeset`, not just the extraction path `tasks.changeset.test.js` already covered). `summarize() changeset exposure` (changeset fields only when meaningful). `list()` (empty-state shape; mixed-status counts; minimal-row schema; newest-first sort). `result()` (final-step `message` only, full `narration`; sums tokens + cost across every step_finish -- issue #201; last-message fallback when no `step_finish reason "stop"`; 2000-char narration truncation with `next` hint; `full: true` returns untruncated; missing log file returns cleanly; running task shows 'still running'; projects only requested fields; rejects `full: true` without `narration` in fields; running task's selected fields return null; unknown summary task doesn't expose partial output). `result() diffStat field` (routes through `git apply --numstat` via `runOverlayCommandFn`; non-git diffs report non-zero file count; parses a real `diff -ruN` patch via the real `git apply --numstat` parser; zero-stat fallback on non-zero exit; non-zero status zeroes the stat even with non-empty stdout). `tail()` (Unicode-safe suffix of latest text event; no-text response; `chars` validation; 131072 ceiling + rejection above; raw capture fallback for eventless crashed task; `none-observed` once any parseable event exists; raw-capture respects `chars` + reports truncation; eventless empty log keeps `none-observed`; watchdog-killed eventless task shows raw capture). `poll()` (immediate for non-running; resolves on real exit; no timeoutMs blocks; `running` after timeout; narration tail on timeout; `mock.timers` no-options never resolves; `mock.timers` timeoutMs above the old 45000 cap still returns `running`). `daemon-restart handling for a check gate that was mid-flight` (Task 7: persisted `checkStatus: "running"` reclassifies to `"interrupted"` on load; already-passed check is left untouched; `changesetStatus !== "pending"` guard leaves a kill-handshake-raced decision alone instead of flipping it; live overlay + a stubbed gate-spawn proves `startCheckGate` is invoked exactly once on the auto re-run and `checkStatus` is back to `"running"`; persisted `checkGatePid` reaps the previous-daemon's orphan via process-group SIGTERM BEFORE the fresh re-run mounts the same overlay). |
| `tasks.lifecycle.diff.test.js` | 27 | `result() diff field` (cached patch text for `fields: ["diff"]`; null for task with no diffPath). Split out of `tasks.lifecycle.test.js` so the parent file stays under the sonarjs 1000-line cap; runs as its own `node --test` entry in `package.json`'s `test:unit` glob. |
| `tasks.advisor.test.js` | 370 | `advisor(): validation, completion, and timeouts` (requires a model; dispatches with the given model/variant + resolves inline once the task finishes; `--executor pi` reaches an actual pi-spawned child end-to-end; returns `status: "running"` with a `task_id` and `session_id` when the timeout elapses first; reports `status: "queued"` not running when the timeout elapses while the task is still waiting for a concurrency slot; timeout before session id is written points at `taskferry wait with task id`; a dispatch validation error is reported under `taskferry advisor`; no `timeout_ms` against an injected small `maxWaitMs` still returns the bounded 'still running' + resumable `session_id` shape). `advisor(): timeout, queueing, and no-timeout shape` (with no `timeout_ms` ... still returns the bounded 'still running' + resumable `session_id` shape). `advisor(): session resume, expiry, and crash surfacing` (fresh `session_id` within TTL is passed through to dispatch via `--continue --session`; expired `session_id` starts fresh + reports `session_reset`; crashed advisor task surfaces `exitCode`/`spawnError`, not a thrown error). |
| `tasks.summarize.test.js` | 797 | `summarize()`: spawn shape, attachment, and snapshot content (uses `--pure` + private attachment; `previous_summary` field; omits when no prior activity; truncated tool call input/output alongside text; no model call when no text observed; rejects unavailable summary model before creating a task; `checkSummaryModelReady` rejects when summary model unavailable; `createTaskManager()`'s real default listModelsFn validates against opencode's list, not the dispatch-default executor's; injected listModelsFn takes precedence; `--mode activity` rejects when summary model unavailable; head+tail narration around an oversized log omission marker). `summarize()`: listModelsFn source-of-truth and oversized-log snapshot (the same opencode-default test, plus a createTaskManager+pi variant; oversized log preservation). `summarize()`: multi-call session continuity (first call spawns with no `--continue`/`--session`; second call continues the prior session + sends only the narration delta). `summarize()`: continue-fail-so-fresh retry (session-id mismatch detected, fresh retry, cache cleared of stale id). `summarize()`: exit handler persistence and concurrency reserve (exit handler persists opencode session id + source-log watermark; activity-refresh summary reserve retries briefly instead of dropping). `summarize()`: per-caller-env listModels cache (Fix 1) (separate entries for callers with different provider keys; identical envs share; cosmetic differences ignored; separates across opencode catalog/auth overrides + provider base-URL overrides; TTL expires per-key; concurrent populates coalesce). |
| `tasks.env.test.js` | 779 | `caller-env union` (sanitizedEnvironment) split into 6 sub-describes by topic so no describe callback exceeds the 200-line cap: basic dispatch and TASKFERRY_TASK_ID; envFileVars merge with caller/ambient; envFile load at construction; envFile watch reload/error/setup/close; summary/advisor/report env forwarding; queue-time env freezing + ambient-read-fresh; single-pass merge + key validation. Covers caller-supplied overlay of daemon ambient; fixed excluded-set (PATH/HOME/`TASKFERRY_STATE_DIR`/...) never overridable; denylist strips even from caller; `TASKFERRY_CHILD` survives denylist; `TASKFERRY_TASK_ID` stamped on dispatch + advisor; absent from summary; omitting `env` is ambient-only; `envFileVars` supplies / loses to ambient / loses to caller; `envFileVars` denylist strips; PATH from `envFileVars` cannot override ambient; `envFileVars` cannot smuggle `TASKFERRY_SOCKET_PATH`/HOME; `envFilePath` loads once at construction; loadEnvFileFn throw propagates synchronously; omitting `envFilePath` skips load; empty `TASKFERRY_ENV_FILE` disables loading; `watchEnvFileFn` starts a live watch; omitting it skips watch; onReload updates `envFileVars` for every dispatch after it fires; onError leaves `envFileVars` at last-known-good; setup failure is caught + logged; `manager.close()` closes the watcher; `summaryEnvironment` strips `OPENCODE_CONFIG*`; advisor forwards caller env; summarize report mode forwards caller env; queued dispatch's stored env is captured at dispatch time, not re-read; queued env is frozen against later caller mutations (verification gate for the b45de81 clone); summary's spawned env reads daemon's ambient at spawn time; excluded/denylist/caller-wins single-pass merge; exclusion set is sourced from `paths.js`'s plumbing export; caller-supplied env key containing `=` / empty / non-string / undefined rejected synchronously. |
| `tasks.executor.test.js` | 491 | `startTask()` writes stdout through `executor.normalizeLogEvent` (Task 7: write-time normalization) (JSON events flagged null are dropped; non-JSON lines preserved verbatim; trailing partial line preserved). `startTask()` spawns the executor's CLI binary (Task 7: executor-driven binary) (pi dispatch spawns `pi`; default dispatch spawns `pi`). `startTask()` merges `executor.sandboxAuthFile().sandboxEnv` into spawnEnv (Task 7: per-executor env overrides) (opencode rewrites `XDG_DATA_HOME`; pi rewrites `PI_CODING_AGENT_DIR` + auth bind destination; sandboxAuthFile receives `sessionId` + `launchDirectory`; fresh dispatch does not pass `sessionId`). `startTask()` resolves the resumed session file via `Array.find` (no break/continue loop) (the pi sessions scan was originally imperative with two `continue` + one `break`; the rewrite uses `entries.find((entry) => ...)` so the loop body has no control-flow jumps). `startTask()` never lets `normalizeLogEvent()` throws escape the stdout handler (Task 7 review fix) (inline path; trailing-fragment path; settling structured `ExecutorNormalizationError`). |
| `tasks.checkgate.test.js` | 203 | `startCheckGate` (Task 5): no `.taskferry.toml` -> no gate spawn, `checkStatus` stays undefined; declared `check` -> second bwrap invocation fired over the same overlay, `checkStatus` "running"; exit 0 -> "passed" with `checkExitCode: 0`; nonzero exit -> "failed" with `checkOutputTail` containing the last 40 lines of combined stdout/stderr (`lastLines` cap); timed-out gate (mocked `setTimeout`, fake `killFn` injected at `ctx.opts.killFn` seam) -> SIGTERM is sent with a NEGATIVE pid (process-group form), proving the gate was spawned `detached: true` and `sendSignalToProcess`'s group-kill reaches the sandboxed workload instead of just the bwrap monitor; trailing-newline `lastLines` regression (45 lines + "\n" must return 40 real lines, not 39 + a trailing blank); 4-byte UTF-8 (`appendBoundedOutput`) byte-cap regression (300 KiB of `🎉` must come out under 256 KiB of UTF-8 bytes, not ~512 KiB measured in UTF-16 code units; tolerant of a mid-codepoint byte-cut that lands in the replacement-char range). `fakeGateChild()` is a bwrap-shaped fake child with separate `stdout`/`stderr` EventEmitters (distinct from `tasks.test-helpers.js`'s `fakeChild()`, which only wires stdout -- the gate reads both streams, so tests need both). |
| `protocol.js` | 418 | `PROTOCOL_VERSION`, `RPC_METHODS`, request/response/error envelope encode/decode, method-name-to-manager-function mapping, `isEnvironment` env-param predicate, and the data-driven `METHOD_PARAMS` spec (per-field `required`/`optional` predicate pairs plus a per-method `extra` cross-field check — the split that keeps `validParams` computational complexity flat) that `validParams`/`isWithinParams` evaluate, plus the task.summary `mode: "activity"` ↔ `env` rejection (the activity path reads cached task activity and spawns nothing, so caller env has no process to land in — surfacing the combination as `INVALID_PARAMS` rather than silently dropping it). `task.dispatch`'s allowed-param spec accepts `noOverlay` and `parentTaskId`; `task.advisor`'s accepts `parentTaskId` (not `noOverlay` -- overlay is mandatory for the advisor role per ADR 0001; the allowed set is the raw-RPC equivalent of args.js's parse-time rejection). `task.accept`'s accepts `force: isBoolean` (default `false`); `task.reject` deliberately does not (force-override is an accept-only path). `RESULT_FIELDS` allows the check-gate fields (`checkStatus`/`checkCommand`/`checkExitCode`/`checkStartedAt`/`checkEndedAt`/`checkOverride`/`checkOutputTail`) and `parentTaskId`/`projectConfigWarning` for `task.result`'s `fields` projection. `task.tail`'s `chars` ceiling is 131072 (raised from 65536 to fit advisor's default 120k-char context budget). `request` envelope validation is split out into `isRequestEnvelope`/`hasVersion`/`isRequestBody` predicates, and the invalid-params error text is picked by `invalidParamsError`. `event.subscribe`'s optional fields now include `all` (boolean); its `extra` cross-field check accepts `all === true` as a third alternative alongside a valid `directory` or `taskId` (taskferry#315). `errorResponse(id, code, message, help, detail)` (Task 6): the optional 5th `detail` argument is the multi-line message body the daemon wants to surface verbatim; defaults to `message` when omitted so older 4-arg call sites stay valid. The client's `validError()` now requires `error.detail: string` and `buildRequestError()` prefers `error.detail` over its synthesized `message + help` body whenever `detail` is set and distinct from `message` -- the round-trip preserves a fix-forward message (multi-line command/exit/output/resume hint) from `manager.accept` through the daemon socket to the CLI's catch-all error print, instead of collapsing it to `message + \n + help`. |
| `events.js` | 58 | Assigns a monotonic sequence number to each emitted event; that's the whole file. |
| `activity.js` | 486 | `activityCacheKey`/cache `refresh()`: bounded head+tail narration snapshot, optional model-summary call, min-interval throttling. |
| `state-lock.js` | 145 | `withFileLock()`: synchronous, `Atomics.wait`-based cross-process exclusive lock; guards the daemon auto-start race. No longer used by `persistTask()`'s writes (taskferry#55) — those now dump the whole in-memory tasks Map straight to a temp file + rename on a debounced flush instead of a read-merge-write, so there's no cross-process race left to guard against. |
| `output.js` | 354 | TOON encoding (non-TTY) and pretty-renderer dispatch (TTY, via `renderPretty()` from `pretty.js`), `leanStatus`/`leanResult`/`projectList`/`projectContext`/`homeView`, hint-string MCP-name migration. `writeToon()` gates on `io.stdout.isTTY`: TTY routes through `renderPretty()`, non-TTY writes plain `encode()` (byte-identical to the pre-pretty TOON shape). `leanStatus()` surfaces `changesetStatus: "pending"` (and `changesetError`) on a non-`--full` `status`/`wait` response when the settled task has one, so an `accept`/`reject` next-step is visible without `--full`. `leanStatus()` also surfaces the check-gate fields (`checkStatus`/`checkCommand`/`checkExitCode`/`checkStartedAt`/`checkEndedAt`/`checkOverride` — `checkOverride` only when truthy, `checkStatus` block only when the gate actually ran rather than the `"none"` default) and `projectConfigWarning` when set, all split out into the `leanCheckGateFields()` helper so `leanStatus()` itself stays under the sonarjs complexity ceiling. Even under `--full`, `trimOverlayDirs()` reduces the task record's `overlayDirs` down to just `root`/`tmpRoot` -- the per-git-subpath `upperDir`/`workDir`/`rwBinds`/`rwFileBinds` breakdown `tasks.js` persists on the task (see that row) is daemon-internal bookkeeping for extraction/cleanup, not something a `status`/`wait --full` caller has any use for. `leanResult`/`computeResultDetail()` never carried `overlayDirs` in the first place, so `result` needed no equivalent trim. |
| `pretty.js` | 231 | Shape-based TTY renderer: `renderPretty(value)` detects one of four shapes (list, doctor, stats, fallback) via `detectShape()` and dispatches to the matching renderer. Uses picocolors (force-enabled, since it's only called on the TTY path) for bold labels, status-colored headings, dim hints, and gray trend tones. Renders task lists grouped by status (running/queued/done/crashed/cancelled order) with borderless `cli-table3` tables, doctor reports with pass/fail glyphs for healthy/installed/isolated checks plus warning/info bullet blocks, and `doctor --stats` with aligned model tables and trend summaries. The fallback renderer covers any other object shape (error values, single-task responses) with bold-labeled scalar fields. Consumed by `writeToon()` in `output.js` on the TTY branch only. |
| `pretty.test.js` | 220 | Tests for `renderPretty()`: four shape dispatches (list/doctor/stats/fallback), list grouping by status with correct ordering, doctor pass/fail glyphs for healthy/installed/isolated/checked, warning/info bullet blocks, stats table alignment, trend direction coloring, and the graceful-degradation paths (missing/empty arrays, missing trend). |
| `opencode-plugin.js` | 189 | OpenCode's native plugin surface: toasts on task state transitions by subscribing to daemon task-state events through `client.js`. |
| `executor.js` | 364 | `WorkerExecutor` abstraction: `opencodeExecutor()`/`piExecutor()` build each CLI's spawn args, summary prompt, log-event normalization, and sandboxed auth-file binding. Both executor objects expose a summary-prompt method, but `tasks.js` currently hardcodes every summary task's `executorId` to `"opencode"` regardless of the originating dispatch's executor — `piExecutor()`'s summary support is unused in practice. `normalizeToolExecutionHeartbeat(evt)` turns `tool_execution_start`/`tool_execution_update` into a minimal `{ type: "tool_progress", part: { type: "tool-progress", tool, toolCallId } }` event instead of the old `null` (dropped entirely) -- pi previously produced zero log growth for the full duration of a single long-running tool call (a slow test suite, a big build), a real gap in what reaches the no-output watchdog's log-growth signal (see `tasks.js`'s watchdog entry above); deliberately payload-minimal so it doesn't inflate `result()`/`summarize()`'s narration the way a full `tool_use` event would -- downstream consumers only key off specific existing type strings, so `tool_progress` is inert everywhere except the watchdog's raw-growth read and `classifyProviderLine`'s parseable-line check. |
| `sandbox.js` | 307 | `bwrap` mount layout: read-only root bind, deny-list (`~/.ssh`, `~/.aws`, `~/.config/gcloud`, `~/.config/gh`, `~/.gnupg`, `~/.claude`, extendable per-install via `TASKFERRY_SANDBOX_DENYLIST`/`sandboxDenylist`, merged with the fixed defaults in `tasks.js`), `allowedDirs` merging, `resolveGitCommonDir`/`resolveGitDir` for worktree gitdir resolution, `checkOverlaySupport` (bwrap >= 0.8 probe), `buildBwrapBaseArgs()` emits the shared prefix (`--ro-bind / /`, `--proc /proc`, `--dev /dev`, `--tmpfs /tmp`, then one `--tmpfs` per denied path) that both `buildBwrapArgs()` (sandboxed dispatch) and `buildMergedViewBwrapArgs()` in `changeset.js` (changeset extraction/apply) start from, `buildBwrapArgs()` opts in to a copy-on-write overlay on `directory` via the `overlay`/`overlayRwBinds` params (mounted with `--overlay-src`/`--overlay`; bwrap >= 0.8 required), plus `overlayRwFileBinds` for writable files outside `directory` (each bound rw with a plain `--bind` right after the overlay binds, since overlayfs mounts are directory-only), `shareNet` flag (default true; every caller, including advisor, keeps `--share-net` -- the sandboxed worker CLI needs outbound network to reach its model provider, and `--unshare-net` blocked that entirely when advisor briefly passed `shareNet: false`), and `runtimeDirWritable` flag (default true; advisor-role dispatches pass false so the daemon's Unix socket inside runtimeDir is read-only and `connect()` fails). The actual bind-scoping decision (worktree-private gitdir + common dir's shared data only, not the whole common dir) lives in `tasks.js`, not here — see taskferry#224/#227. |
| `changeset.js` | 619 | Overlay path helpers (`subOverlayPaths()` for directory sub-overlays, `subFilePaths()` for the scratch-copy rw bind a writable FILE like a worktree's `packed-refs` needs instead, since overlayfs mounts are directory-only), pre-dispatch HEAD resolution (unborn HEAD anchors on the empty tree), git/non-git diff extraction from the CoW overlay (fail-closed on extraction failure only; HEAD drift is no longer fatal -- `extractGitDiff()` writes the diff unconditionally and returns `headDrift: null | {from, to, recovered: true|false|null, conflictDetail}` via `detectHeadDrift()` + `resolveHeadDrift()`, since a stale-base diff is still valid content that `git apply --3way` can forward-apply onto the new HEAD -- see taskferry#261), changeset apply (`git apply --3way` unconditionally for git targets, so a stale-base diff forward-applies onto new HEAD instead of erroring; in-sandbox rsync --delay-updates for non-git, with required live-overlay inputs validated before a non-git apply), and overlay cleanup (a containment guard refusing roots outside the overlay tmp root, then a same-uid `chmod -R u+rwX` before `rm -rf` -- overlayfs's kernel-owned `work/main/work` scratch dir comes back mode 000, and once anything was ever deleted inside the overlay it holds an internal whiteout entry, making it non-empty; a non-empty mode-000 dir forces `rm -rf` to `opendir()` it (unlike an empty one, which removes via rm's plain-rmdir() fast path), which mode 000 blocks -- `rm -rf` then aborts partway through, having already deleted `upper/`, silently stranding a task that still thinks its overlay is live (taskferry#273); `chmod -R` first sidesteps this since chmod needs only the parent's permissions to retarget an entry, not the entry's own). Both extraction functions, and `applyNonGitChangeset()`'s own overlay-mount bwrap call (the non-git apply's writable rsync remount, review finding from #327), run their bwrap spawn through `runExtractionBwrap()`, a bounded retry-with-backoff (100/300/900ms, `OVERLAY_MOUNT_BUSY_PATTERN`-gated so only the confirmed overlay-mount race retries -- any other bwrap failure still throws on the first attempt) that absorbs a kernel-internal race: the extraction (or apply) bwrap's own overlay mount can lose to the async teardown of the just-exited dispatch/accept bwrap's mount namespace, which has no user-space signal to wait on (taskferry#326; live-verified against real bwrap on a nested-worktree repro -- unpatched `main` hit the exact "Device or resource busy" crash, the retry absorbed it). `applyNonGitChangeset()` needs no extra fail-open guard the way `extractNonGitDiff()` does: rsync's exit codes don't share `diff -ruN`'s "non-zero can mean success" convention, so a persistent busy failure after retries exhaust already falls through to the existing non-zero-status failure branch. `OVERLAY_MOUNT_BUSY_PATTERN` itself now lives here (was duplicated in `tasks.js`, which imports it) since both the retry gate and `tasks.js`'s post-throw `failureReason` classifier need the same regex. `isOverlayMountBusy()` checks the busy pattern against `stderr` and `error?.message` independently (never via `stderr || error?.message`) since a hung mount that trips `spawnSync`'s 30s timeout reports `{error: ETIMEDOUT, stderr: <busy text>}`, and the generic ETIMEDOUT message would otherwise win a naive fallback and hide the busy signature actually sitting in stderr. `extractNonGitDiff()` also runs this same busy check explicitly against the exhausted-retry result even when its exit status is 1 -- bwrap's own die()-on-setup-failure convention exits 1, the exact code `diff -ruN` uses for "differences found," so without the explicit check a persistent overlay-mount-busy failure reads as a successful zero-change diff and silently discards the worker's real edits (`extractGitDiff()` doesn't need the same guard since it already throws on any non-zero status). Also exports `defaultRunCommand`, the 30s diff/apply runner that's deliberately separate from sandbox.js's 5s version-probe runner, and sets an explicit 200 MiB `maxBuffer` on its `spawnSync` call rather than relying on Node's 1 MiB default, since a real merge diff routinely exceeds that and used to throw `ENOBUFS` (surfacing as `taskferry result --diff` silently returning `diff: null`, taskferry#358). `buildMergedViewBwrapArgs()` mounts the overlay's merged view on a synthetic mountpoint under /tmp so a non-git extraction can `diff -ruN` `directory` against its own merged view; the overlay paths (`upperDir`/`workDir`/`mergedMountPoint`, all under /tmp by construction) need no explicit /tmp-shadowing protection — `upperDir`/`workDir` are host-namespace paths consumed by the kernel's overlay `mount(2)` and bypass the bwrap namespace's mounts, and `mergedMountPoint` is intentionally created under the bwrap tmpfs before being overlay-mounted — shares its scaffolding with `buildBwrapArgs()` via `sandbox.js`'s `buildBwrapBaseArgs()`. `extractGitDiff()` no longer throws on HEAD drift (the pre-retry check and the post-retry re-check from taskferry#329 were both removed): the diff is always written, and `detectHeadDrift()` runs exactly once after the diff is persisted; on drift, `resolveHeadDrift()` runs a real `git apply --3way` (never `--check`, whose exit code can't tell a clean merge from a real conflict) inside a disposable detached worktree off the live `directory` -- `directory` is read-only as `worktree add`'s source, never written; the scratch worktree is removed unconditionally after the probe (the `apply`/`status`/conflict-check body runs inside a `try/finally` so the `worktree remove --force scratchDir` always runs once `worktree add` has succeeded, including when an injected `runCommand` throws; the `worktree add` failure path correctly skips cleanup because nothing was created). `resolveHeadDrift()` returns `{recovered: true}` on a clean 3-way merge, `{recovered: false, conflictDetail}` on a genuine conflict (decided by `git status --porcelain` conflict markers, not by apply's exit code), `{recovered: null, conflictDetail}` when the worktree add itself fails (no apply attempted), and `{recovered: null, conflictDetail}` when the `git status --porcelain` inspection itself failed (spawn error / non-zero exit) -- empty stdout from a failed status call is "didn't get to read porcelain", not "no conflict markers", so the empty/absent match isn't trusted. `extractGitDiff()` exposes an injectable `scratchDirFn` (default `os.tmpdir()/taskferry-stale-base-<uuid>`) so callers/tests can pin or mock the disposable worktree root. The apply/status/conflict-classification body is split into a private `evaluateApplyAndStatus()` helper to keep `resolveHeadDrift()` under the file's cyclomatic-complexity cap. `runExtractionBwrap()`'s `sleepFn` (real blocking `Atomics.wait`-based `sleepSync` by default) is now reachable from outside this module: `tasks.js` threads an `overlaySleepFn` manager option down to both `extractChangesetForTaskRecord()`'s `extractGitDiff`/`extractNonGitDiff` calls and `acceptTaskChangeset()`'s `applyChangeset` call (undefined by default, so production behavior/blocking is unchanged), letting `tasks.changeset.test.js`'s overlay-mount-busy reclassification tests inject a fast no-op instead of eating the real ~1.3s retry backoff on every run (taskferry#328). |
| `config.js` | 174 | `loadConfig()`: reads/validates `config.json` against `CONFIG_FIELD_TYPES`, rejects unrecognized keys, with mtime-based per-path caching so repeated calls in the same process only stat the file instead of re-reading/parsing. `advisorContextChars` controls advisor's auto-attached context budget. `envFile` names the path `tasks.js` loads via `env-file.js`'s `loadEnvFile()`. `providerLimits` is the one config field shaped as a nested object rather than a scalar: `validateProviderLimits()` mirrors the top-level object/key/type checks one level deeper, accepting a flat map of provider name -> `{maxConcurrentTasks?, maxDispatchesPerWindow?}` (both optional positive integers) and returning it verbatim — normalization into the scheduler's `Map` shape happens later in `tasks.js`. `_resetConfigCache()` exported for test use. |
| `project-config.js` | 172 | `loadProjectConfig()`: reads/parses/validates `.taskferry.toml` against a fixed `KNOWN_KEYS` allow-list (`check`/`check_timeout_seconds`/`read_only_paths`) via `smol-toml`, with the same mtime-based per-path caching shape as `config.js`'s `_configCache` so the three independent call sites in a task's lifecycle (dispatch-time prompt injection, spawn-time read-only binds, settle-time gate) each pay only a `stat()` when the file hasn't changed since the last read. Never throws: a missing file returns an `EMPTY_CONFIG`-shaped result (no gate, no injection, no extra binds), and an unparseable or schema-invalid file also returns `EMPTY_CONFIG` with `parseError` set so the caller surfaces a loud warning instead of guessing a partial config -- dispatch proceeds with no gate, per the design's error table. `verificationPromptBlock()` is the always-on prompt block appended to a dispatch's prompt when the project declares a check command. `resolveReadOnlyProjectBinds()` is the shared, mount-order-safety-checked resolver for `read_only_paths` (Task 4): filters project-supplied entries to safe `[src, dest]` ro-bind pairs, dropping (and reporting) entries that don't exist on the host or that overlap any protected mount -- equals it, is an ancestor of it, or is a descendant of it. The `overlaps` predicate uses a `childPrefix(base)` helper that returns `base` unchanged when `base === path.sep` so `"/" + path.sep` doesn't become the broken `"//"` prefix -- a literal `read_only_paths = ["/"]` is therefore correctly classified as an ancestor of every protected path and rejected, not slipped through as `--ro-bind / /` (which would shadow the deny-list tmpfs mounts AND the overlay mount). Both the spawn-time `buildBwrapBinds` (worker) and Task 5's settle-time gate call this exact function, so the worker and the gate always see the same read-only mount surface. `resolveProjectConfigPath()` locates `.taskferry.toml` under a dispatch's working-tree root; `_resetProjectConfigCache()` exported for test use. |
| `env-file.js` | 247 | `parseEnvFile()`/`loadEnvFile()`: minimal `.env`-style parser (`NAME=VALUE` per line, `#` comments, optional `export `, matching single/double quotes stripped, unterminated quotes rejected rather than returning the quote character as literal data) for the file named by `TASKFERRY_ENV_FILE`/`envFile`. Throws with a file:line-numbered message on a malformed line (deliberately omitting the line's own raw content from every parse-error message, since a malformed line's content can itself be a secret — e.g. a bare API key pasted with no `NAME=` prefix — and that message can end up in `daemon-boot.err`, which callers/cron routinely capture into their own logs) or (in `loadEnvFile()`) a missing file or a file readable by group/other (`chmod 600` required) — `tasks.js` calls this once at `createTaskManager()` construction, so a bad path, malformed file, or insecure mode fails daemon startup loudly rather than silently dispatching without the secrets it was meant to supply. `watchEnvFile()`: keeps `envFileVars` live after the startup load by watching the parent directory (filtered by basename, so an mktemp+rename secret rotation that swaps the file's inode still fires) on a ~100ms debounce; a torn read caught mid-rewrite is rejected by comparing pre-/post-read `mtime`/`size` (reschedules instead of handing `onReload` a possibly-truncated `{}` that would silently wipe every previously loaded secret); a `null`/`undefined` `changedName` from platforms that don't report filenames falls through the name filter rather than being treated as a match (extra debounced re-reads of unrelated directory activity are harmless thanks to the mtime/size guard above); `onReload` itself is wrapped in a try/catch so a throwing callback becomes an `onError` warning instead of an uncaught exception inside the debounced `setTimeout`. |
| `mcp-isolation.js` | 115 | Playwright MCP isolation checks for `taskferry doctor`/`setup` (`opencode.jsonc`, Claude Code's Playwright MCP config). |
| `doctor-stats.js` | 156 | `computeDoctorStats()`: pure aggregation of `task.list` rows into the `doctor --stats` report shape — status mix (overall/24h/7d), per-model dispatch/rate breakdown, failure-reason histogram, unknown backlog, and 24h crash-rate trend. |
| `paths.js` | 222 | Resolves `TASKFERRY_STATE_DIR`/`TASKFERRY_RUNTIME_DIR`/`TASKFERRY_CACHE_DIR` and the socket path from XDG defaults + env overrides. Exports `TASKFERRY_PLUMBING_ENV_VARS` (frozen array of those five names, now including `TASKFERRY_OVERLAY_TMP_DIR`) so `tasks.js` builds its caller-env exclusion set from the same source of truth — a new plumbing var added here lands in the exclusion set automatically. `resolveOverlayTmpRoot()` scopes the CoW overlay root under `resolveRuntimeDir()` (not plain `os.tmpdir()`) so two daemon instances — isolated via `TASKFERRY_STATE_DIR`/`RUNTIME_DIR` or not — never share an overlay namespace; a restarting/crash-looping daemon's startup orphan sweep used to be able to delete a *different* daemon's in-flight overlay because every daemon on the host shared plain `/tmp` regardless of state/runtime isolation (taskferry#286). `TASKFERRY_OVERLAY_TMP_DIR` is an explicit escape hatch to pin the overlay root elsewhere. Also `resolveWorkspaceRoot()`: the git workspace root (handles plain repo/worktree/submodule/bare-repo layouts) that `list`/`watch`/`context`/`home` default `--directory` to — `dispatch`/`advisor` deliberately keep their default on literal cwd instead, since that value doubles as the sandbox root. `createWorkspaceRootResolver()` memoizes `resolveWorkspaceRoot()` per directory (one instance created per `startDaemon()` call in `daemon.js`, so its cache is scoped to that daemon's lifetime, not a module-level global) so repeated directory comparisons don't re-spawn `git` each time; `sameWorkspace(a, b, resolveRoot)` is the shared comparison both `daemon.js`'s `filteredTaskDetails()`/`filteredList()` and `daemon-server.js`'s `deliverEvent()` use instead of a raw `===`, so a repo-root-scoped `list`/`context`/`watch` also matches a task dispatched into one of that repo's linked worktrees (taskferry#315) — the literal-equality fast path inside it skips calling `resolveRoot` (and, on a cache miss, spawning git) for the common same-directory case. |
| `narration-format.js` | 24 | Formats a task's narration/activity text for display. |
| `errors.js` | 20 | Shared error-message helpers. |
| `numbers.js` | 14 | Shared numeric-parsing helpers (`positiveInteger`, `nonNegativeInteger`, etc.). |
| `setup.js` | 268 | `taskferry setup`: npm install, managed symlinks, per-client integration registration (see `.superpowers/.completed/specs/2026-07-16-taskferry-setup-design.md`). |
| `init.js` | 105 | `taskferry init`: `detectCheckCommand()` shallowly detects package scripts or ecosystem marker files (malformed package JSON falls through, while filesystem read errors propagate); `runInit()` scaffolds `.taskferry.toml` at literal cwd, never overwrites an existing file, asks before enabling a detected command on a TTY through an injectable readline factory, and otherwise leaves the detected proposal commented out. |
| `init.test.js` | 141 | Unit coverage for ecosystem detection, malformed-versus-unreadable package manifests, no-overwrite behavior, TTY confirmation/rejection, and non-TTY commented templates. |
| `tf-sl.sh` | 113 | `tf-sl` Claude Code statusline segment: reads the statusline JSON from stdin, emits a width-responsive `tf: ...` ANSI segment or nothing. Always calls `taskferry` with `TASKFERRY_AUTO_START=0` — a statusline poll must never be the call that boots the daemon, since the daemon inherits its first caller's env for its whole lifetime. |
| `scripts/generate-skill.js` | — | Regenerates `integrations/*/skills/using-taskferry/SKILL.md` from `skills/using-taskferry/SKILL.md`; `--check` fails on drift. The two generated copies are committed, not gitignored — they're what the Claude Code and Codex plugin marketplaces actually read (`integrations.test.js` pins the plugin `source` to those exact paths), so a missing or stale copy ships wrong skill content to real installs, not just a rebuildable artifact. |

Most `*.js` files above have a co-located `*.test.js` (`node --test`, no
framework) — `mcp-isolation.js`, `narration-format.js`,
`errors.js`, and `numbers.js` are the current exceptions.
`smoke-test.js` (129 lines) / `cancel-smoke-test.js` (105) /
`poll-smoke-test.js` (78) are integration tests that spawn a real daemon,
CLI, and (via the default `pi` executor) a live dispatch to
`minimax/MiniMax-M3` (`npm run test:integration`, not part of the default
`npm test`). They run in CI as the separate `integration` job in
`.github/workflows/check.yml` (needs the `pi` CLI installed, plus
`MINIMAX_API_KEY` in the environment; skipped on fork PRs, which don't
receive repo secrets). That job builds `bubblewrap` from source rather than
`apt install`ing it — Ubuntu's apt package reports a version that
`checkOverlaySupport()` (in `sandbox.js`) trusts as overlay-capable, but is
actually built with `--overlay-src`/`--overlay` compiled out, so every
sandboxed dispatch would otherwise fail with `bwrap: Unknown option
--overlay-src`; a distro package's reported version number doesn't
guarantee the flags it was compiled with are present.
`smoke-test-support.js`
(58 lines) holds their shared teardown helpers: `stopDaemonAndWait()` blocks
until the daemon's SIGTERM-triggered exit actually completes instead of
racing it, and `rmRoot()` shells out to `rm -rf` rather than `fs.rmSync`
because a sandboxed dispatch's overlay mount leaves a kernel-zeroed
(mode `0000`) overlayfs workdir behind — `fs.rmSync` doesn't chmod an
inaccessible-but-owned directory before recursing into it and throws EACCES
every time, but GNU `rm -rf` already handles this case.

## Where do I look for X?

| Question | Look here |
|---|---|
| What does this CLI flag do? | `docs/cli-reference.md` |
| Why did a task crash / how do I read `failureReason`? | `docs/troubleshooting.md`, `docs/daemon.md#watchdogs` |
| Where is the closing `Status:` marker parsed into `finalStatus`? | `tasks.js`'s `evaluateOutputCompleteness()` + `STATUS_MARKER_RE`; surfaced via `summarizeOptionalFields()` and `computeResultDetail()`; allow-listed in `protocol.js`'s `RESULT_FIELDS` |
| Where is `--class` validated, persisted, and surfaced? | `protocol.js`'s `isNonEmptyString` (dispatch + advisor params); `tasks.js`'s `dispatch()` persists on the task record; `computeResultDetail()` and `status` surface it; `result --fields class` projects it |
| Where is `--parent-task` validated, persisted, and surfaced? | `protocol.js`'s `isNonEmptyString` (dispatch + advisor params); `tasks.js`'s `buildDispatchTask()`/`dispatchAdvisorTask()`/`runAdvisor()` thread it as `parentTaskId`; `summarizeOptionalFields()` + `computeResultDetail()` + `output.js`'s `leanStatus()` surface it; `result --fields parentTaskId` projects it |
| Where is `--force` (accept override) wired end-to-end? | `args.js`'s `--force` boolean (accept only); `protocol.js`'s `force: isBoolean` for `task.accept`; `commands.js`'s `runAccept()` forwards `force: true` only when set; `daemon.js`'s `invoke()` calls `manager.accept(taskId, { force })`; `tasks.js`'s `acceptTaskChangeset()` (Task 6: now `async`) honors `force`: skips the gate-blocking branches in `validateAcceptable()` (calls `validateCheckGateAcceptable()` after the gate-specific check), `await ctx.killGateAndWait(taskId)` if a gate is still running, sets `task.checkOverride = true` when force + a blocking checkStatus was in effect. Reject's kill handshake uses the same `ctx.killGateAndWait` supervisor. `buildCheckGateFailureMessage(task)` formats the multi-line fix-forward error for failed/timeout/interrupted gates (command/exit/timeout/interrupted line, indented output tail, `--session-id`/`--directory` resume hint). |
| Where are the check-gate fields (`checkStatus`/`checkCommand`/`checkExitCode`/`checkStartedAt`/`checkEndedAt`/`checkOverride`/`checkOutputTail`/`projectConfigWarning`/`parentTaskId`) projected through the result/status surfaces? | `tasks.js`'s `summarizeOptionalFields()` (lean TaskSummary), `summarizeCompletionFields()`/`summarizeExecutionFields()`/`summarizeCheckGateFields()` (bucket splits for the new fields), `computeResultDetail()` + `resultCheckGateFields()` (full ResultDetail, with `checkOutputTail` gated on the caller's `fields` selection); `output.js`'s `leanStatus()` + `leanCheckGateFields()` for the lean status surface; `protocol.js`'s `RESULT_FIELDS` allow-list. Task 5 implements the runner that fills these fields in (`startCheckGate` fires from `extractChangesetForTaskRecord`'s `hasChanges` branch, gated on `isGitTarget`, spawning the project's `check` command inside the worker's same overlay mount via `ctx.env.startCheckGate`); Task 6 wires the supervisor that consumes them. |
| Where is the `.taskferry.toml` `check` command run, and how does the gate keep up with the worker/gate lifecycle? | `tasks.js`'s `startCheckGate()` (`extractChangesetForTaskRecord` `hasChanges` branch + `isGitTarget` guard, fires after extraction completes), with the live `ChildProcess` tracked on `ctx.maps.gateChildren` (new Map alongside `tasks`/`waiters`/`advisorSessions`); `wireCheckGateChildHandlers()` attaches stdout/stderr/exit/error listeners + the timeout-escalation timer; `killGateAndWait(taskId, ctx)` (Task 6's actual user, but the binding is built here as `ctx.env.killGateAndWait`) sends process-group SIGTERM and waits for actual exit (escalates to SIGKILL after `CHECK_GATE_KILL_GRACE_MS`); `lastLines(text, n = 40)` and `appendBoundedOutput(tail, chunk)` are the tail-cap (`checkOutputTail` last-N-lines contract) and bounded-buffer (`CHECK_GATE_OUTPUT_CAP_BYTES = 256 KiB`) helpers. `markCheckGateFailedWithError()` is the shared sync-throw + async-`error` failure sink. Detached-spawn rationale and the bwrap `--unshare-pid` namespace-teardown myth: see the "Kill mechanism" review note in `startCheckGate`'s doc comment. |
| Where does a daemon-restart mid-gate get cleaned up, and how does the auto re-run of a surviving gate work? | `tasks.js`'s `markInterruptedGatesFor()` (Task 7) — reclassifies `checkStatus: "running"` → `"interrupted"` at boot, then if the task's overlay is still live, best-effort process-group SIGTERMs the persisted `task.checkGatePid` (the orphan from the prior daemon, since the gate was spawned `detached: true` and the pid IS the group leader) before auto-re-invoking `ctx.env.startCheckGate(task)`. The `changesetStatus !== "pending"` guard inside the loop prevents a kill-handshake-raced decision (the kill fired but the exit event hadn't landed when the daemon died) from being flipped to "interrupted" on every future restart. Wired into `bootstrapManagerContext` right after `sweepOrphanedOverlays()`; test in `src/tasks.lifecycle.test.js`'s `daemon-restart handling for a check gate that was mid-flight` describe block. |
| Where is a pending overlay changeset accepted or rejected? | `daemon.js`'s `invoke()` RPC routing; `tasks.js`'s `accept()`/`reject()`; `changeset.js`'s `applyChangeset()`/`cleanupOverlay()` |
| Where is a stale-base head-drift settled into `changesetStatus` (recovered: pending, conflicting: auto-reject, inconclusive: pending + infra error)? | `tasks.js`'s `extractChangesetForTaskRecord()` -> `settleChangesetAfterExtraction()`; the drift classification itself comes from `changeset.js`'s `detectHeadDrift()` + `resolveHeadDrift()` (Task 3) |
| Daemon lifecycle, socket resolution, protocol envelope | `docs/daemon.md` |
| What does the daemon pass to a worker's environment? | `tasks.js`'s `sanitizedEnvironment()`/`dispatchEnvironment()`/`summaryEnvironment()`; the `envFile`/`TASKFERRY_ENV_FILE` base layer is loaded once via `env-file.js`'s `loadEnvFile()` |
| What does the daemon send to a summary model, how to disable it | `docs/security.md` |
| Retired MCP tool names / flags | `docs/migrating-from-mcp.md` |
| Per-agent (Claude Code/Codex/OpenCode) setup | `docs/integrations/*.md` |
| Open design questions, past decisions, what's left to build or deliberately skipped | `.superpowers/specs/*.md`, `.superpowers/plans/*.md` (implemented ones move to `.superpowers/.completed/`) |
| The canonical agent-facing skill (regenerate after any CLI-surface change) | `skills/using-taskferry/SKILL.md`, then `npm run skill:generate` |
| User-tunable options via a JSON config file (as an alternative to env vars) | `docs/config.md` |
| Why `list`/`watch`/`context`/`home` default to the git workspace root but `dispatch`/`advisor` don't, and why `init` scaffolds literal cwd | `docs/cli-reference.md`, `paths.js`'s `resolveWorkspaceRoot()`, `cli.js`'s local `init` bypass |
| Why a root-scoped `list`/`watch`/`context` also sees a task dispatched into a linked worktree; the `watch --all`/`list --all` "every workspace" escape hatch | `paths.js`'s `createWorkspaceRootResolver()`/`sameWorkspace()`, `daemon.js`'s `filteredTaskDetails()`, `daemon-server.js`'s `deliverEvent()`/`ALL_DIRECTORIES` (taskferry#315) |
| How is `.taskferry.toml` scaffolded for a project? | `init.js`'s `detectCheckCommand()`/`runInit()`; `cli.js`'s `runInitCommand()` passes literal cwd and bypasses the daemon client |
| Where a project's `.taskferry.toml` is loaded/validated/cached, the always-on prompt block that injects the declared check command, and how `read_only_paths` becomes validated bwrap ro-bind pairs (including the mount-order safety check that rejects `read_only_paths = ["/"]` before it could shadow the deny-list tmpfs mounts or the overlay mount) | `project-config.js`'s `loadProjectConfig()`/`verificationPromptBlock()`/`resolveReadOnlyProjectBinds()`/`resolveProjectConfigPath()` (the `overlaps` predicate uses a `childPrefix()` helper that returns the base unchanged when `base === path.sep`, so `"/"` is correctly recognized as an ancestor of every protected path); `tasks.js`'s `dispatchTask()` calls `loadProjectConfig(normalizedDirectory)` and (for `role: "dispatch"` only) appends `verificationPromptBlock(projectConfig.check)` to the prompt before both `buildDispatchTask()` and `queueDispatchLaunch()`; `tasks.js`'s `applyProjectConfigReadOnlyBinds()` (called from `buildBwrapBinds`) runs `resolveReadOnlyProjectBinds()` with the protected mount set (`denyList` + `stateDir` + `runtimeDir` + `launchDirectory`) and appends the safe pairs to the dispatch's `extraRoBinds`, surfacing dropped entries (missing-on-host or overlapping) via `task.projectConfigWarning` |
| Advisor's auto-attached context budget / Claude transcript path resolution / user-assistant-only extraction | `commands.js`'s `resolveAdvisorContextChars()`/`claudeTranscriptPath()`/`extractTranscriptText()` |
| Advisor `--summarize-context` condensation path (throwaway dispatch+wait+result, best-effort) | `commands.js`'s `summarizeContextText()`, gated on `args.js`'s `--summarize-context` boolean |
| `watch --flush-interval` batching, Monitor auto-arm convention | `docs/cli-reference.md` |
| Per-RPC-request latency / catching a daemon load-time spike | `<state-dir>/perf.log`, `docs/daemon.md#request-latency-profiling`, `daemon.js`'s `makeRequestTimer()` |

## Env vars

All `TASKFERRY_*` vars the daemon or CLI reads, gathered in one place
(individual docs above cover behavior; this is just the index):

Vars marked "config.json" also have a config-file equivalent — see
`docs/config.md` — where the env var, if set, still takes precedence.

| Var | Default | Config file? | Purpose |
|---|---|---|---|
| `TASKFERRY_STATE_DIR` | `$XDG_STATE_HOME/taskferry` or `~/.local/state/taskferry` | no | Task state, logs, summary prompts |
| `TASKFERRY_RUNTIME_DIR` | `$XDG_RUNTIME_DIR/taskferry`, or `/run/user/<uid>/taskferry` if that var is merely unexported but the dir exists, or `<state-dir>/run` | no | Socket + lock files |
| `TASKFERRY_SOCKET_PATH` | `<runtime-dir>/daemon.sock` | no | Explicit socket override |
| `TASKFERRY_CACHE_DIR` | `$XDG_CACHE_HOME/taskferry` or `~/.cache/taskferry` | no | Real-disk data home for sandboxed workers (opencode/pi auth + unbounded snapshot caches); see `docs/security.md` |
| `TASKFERRY_OVERLAY_TMP_DIR` | `<runtime-dir>/overlay` | no | CoW overlay root for dispatch writes; scoped under the runtime dir (not plain `os.tmpdir()`) so isolated daemon instances never share an overlay namespace -- taskferry#286 |
| `TASKFERRY_AUTO_START` | `1` (auto-start enabled) | no | Set to `0` to stop the CLI from auto-spawning a daemon on first use |
| `TASKFERRY_DISABLE_SANDBOX` | `0` (sandboxed on Linux) | yes (`sandboxEnabled`, inverted) | Set to `1`/`true` to run dispatches without the bwrap filesystem sandbox |
| `TASKFERRY_DISABLE_OVERLAY` | `0` (overlay enabled) | yes (`overlayEnabled`, inverted) | Set to `1`/`true` to disable the CoW overlay for dispatch writes |
| `TASKFERRY_ALLOWED_DIRS` | — | yes | Extra directories bound read-write inside the sandbox for every dispatch; see `docs/security.md` |
| `TASKFERRY_CANCEL_GRACE_MS` | `5000` | yes | Default SIGTERM→SIGKILL grace period for `cancel`, overridden per-call by `--grace-ms` |
| `TASKFERRY_DEFAULT_EXECUTOR` | `pi` | yes | Default `--executor` (`opencode` or `pi`) when a dispatch/advisor call omits it |
| `TASKFERRY_MAX_CONCURRENT_TASKS` | `4` | yes | Running-task concurrency cap |
| `TASKFERRY_MAX_DISPATCHES_PER_WINDOW` / `TASKFERRY_DISPATCH_WINDOW_MS` | `2` / `5000` | yes | Dispatch burst-rate limit |
| `TASKFERRY_ENV_DENYLIST` | — | yes (`envDenylist`) | Comma-separated environment variable names stripped from spawned children after caller-env forwarding |
| `TASKFERRY_ENV_FILE` | — | yes (`envFile`) | Path to a `.env`-style file loaded once at daemon startup and unioned in as the lowest-priority layer of every spawned child's environment, below the daemon's ambient env and the caller's forwarded env; see `docs/security.md#caller-env-forwarding` |
| `TASKFERRY_SANDBOX_DENYLIST` | — | yes (`sandboxDenylist`) | Comma-separated extra directories tmpfs-masked inside the bwrap sandbox, merged with (not replacing) the fixed default deny-list; see `docs/security.md` |
| `TASKFERRY_NO_OUTPUT_TIMEOUT_MS` | `256000` (~4.3 min) | yes | Pre-output-seen watchdog deadline |
| `TASKFERRY_POST_OUTPUT_NO_OUTPUT_TIMEOUT_MS` | `400000` (~6.7 min) | yes | Watchdog deadline once a task has produced its first log event |
| `TASKFERRY_WATCHDOG_POLL_MS` | `2000` | no | Watchdog check interval |
| `TASKFERRY_WATCHDOG_GRACE_MS` | `5000` | yes | SIGTERM→SIGKILL escalation grace period when the watchdog force-stops a task (same override surface as `cancel`'s `--grace-ms`, but for watchdog-triggered stops) |
| `TASKFERRY_SUMMARY_MODEL` | `opencode/mimo-v2.5-free` | yes | Model behind `summary --mode report` |
| `TASKFERRY_ACTIVITY_SUMMARIES` | `true` | yes | Enables `watch --summaries` / activity-style model calls |
| `TASKFERRY_SUMMARIZER_TIMEOUT_MS` | `360000` (6 min) | yes | Throttle between activity-summary model calls |
| `TASKFERRY_ACTIVITY_MAX_WORDS` | `75` | yes | Max words in an activity-style summary |
| `TASKFERRY_ADVISOR_SESSION_TTL_MS` | `1800000` (30 min) | yes | Advisor session idle expiry before auto-reset |
| `TASKFERRY_WAIT_DEFAULT_TIMEOUT_MS` | `900000` (15 min) | yes | Default timeout for `wait` and `summary --wait`; set to `0` to disable |
| `TASKFERRY_ADVISOR_CONTEXT_CHARS` | `120000` | yes | Advisor auto-attached context budget in chars; env var wins over config file's `advisorContextChars` |
| `TASKFERRY_ADVISOR_SUMMARIZER_MODEL` | `opencode/mimo-v2.5-free` | no | Model used by `advisor --summarize-context` to condense the auto-attached context; falls back to the constant in `commands.js` when unset |
| `TASKFERRY_CHILD` | — | no | Set on the daemon's own spawned children; see `docs/security.md` |
| `TASKFERRY_PROFILING_ENABLED` | `0` (disabled) | yes (`profilingEnabled`) | Set to `1`/`true` to time every RPC request and write `<state-dir>/perf.log`; see `docs/daemon.md#request-latency-profiling` |
| `TASKFERRY_SLOW_REQUEST_MS` | `500` | no | Duration threshold past which a request's `perf.log` entry also gets a `slow request: ...` stderr line (only takes effect when profiling is enabled) |
| `TASKFERRY_PERF_LOG_MAX_BYTES` | `5242880` (5 MiB) | no | Size at which `perf.log` rotates to `perf.log.1` (only takes effect when profiling is enabled) |

## Things that look like bugs but aren't

- `status: "unknown"` after a daemon restart — expected; see
  `docs/daemon.md#recovery`. There is deliberately no re-attachment to
  already-running child processes.
- `checkStatus: "interrupted"` on a task after a daemon restart that killed
  the previous daemon mid-gate — expected (Task 7); the only way out is to
  re-run the gate (auto re-run if the overlay survived, or accept with
  `--force` after manual verification if the gate is the wrong gate to
  re-run). The pre-fix alternative was `checkStatus: "running"` forever,
  with no settle path and no error message, and `accept --force` blocked
  by Task 6's gate-supervisor because the gate was technically still
  "running".
- `taskferry wait` returning with `status: "running"` and a `note` about
  timing out — expected when the 15-minute default timeout (or an explicit
  `--timeout`) elapses before the task settles; re-run `taskferry wait`
  to keep polling or pass `--timeout` for a longer cap.
- A `SKILL.md` edit not showing up in `integrations/claude/skills/...` —
  run `npm run skill:generate`; the distributed copies are generated, not
  hand-edited. Commit them alongside the canonical file anyway — they aren't
  build output regenerated at install time, they're the literal content each
  plugin marketplace serves from this repo's git history.
- Editing `~/.claude/skills/using-taskferry/SKILL.md` directly does nothing for
  this repo — it's a separate manual copy for global availability outside
  the plugin (see `docs/integrations/claude-code.md`), not synced from or
  to the canonical `skills/using-taskferry/SKILL.md`. Edit the canonical file,
  run `npm run skill:generate`, then re-copy to `~/.claude/skills/` by hand.
- The daemon restarting itself with no `taskferry` command involved — expected
  when a source `.js` file's mtime moved forward since startup (a merge
  landed) and no tasks were running/queued; see
  `docs/daemon.md#self-restart-on-source-change`.
- Editing `TASKFERRY_ENV_FILE`'s target file and a spawned worker not seeing
  the new/changed var right away — expected within a short debounce window,
  not a restart. `envFileVars` is loaded once via `env-file.js`'s
  `loadEnvFile()` at `createTaskManager()` construction (daemon startup), then
  kept live by `env-file.js`'s `watchEnvFile()`, which watches the file's
  parent directory (filtered by basename, to survive a mktemp+rename secret
  rotation swapping the file's inode) and re-runs `loadEnvFile()` on a change,
  debounced ~100ms — no daemon restart required. A reload that fails to parse,
  or a torn read caught mid-rewrite, is reported via `onError` (a stderr
  warning, itself EPIPE-guarded) and the previous `envFileVars` is kept
  as-is. Editing the `envFile` config key itself (not the file it points at),
  or the running daemon's own launch environment, still requires a restart —
  those are read once at `createTaskManager()` construction, same as
  `envDenylist` and the other config-file-backed options. This is unlike a
  caller-forwarded key, which does take effect immediately on the very next
  `dispatch`/`advisor`/`summary` call with no restart (see
  `docs/security.md#caller-env-forwarding`) — the three mechanisms (env-file
  hot-reload, config-key restart, caller-forwarded immediacy) solve different
  problems and have different freshness guarantees on purpose.
- A pending changeset listing files the worker never touched — expected when
  the dispatch directory had untracked files at dispatch time. Git-target
  extraction stages the overlay's whole merged view (`git add -A && git diff
  --cached <pre-dispatch HEAD>`, `changeset.js`'s `extractGitDiff`) so
  pre-existing untracked files surface as new-file entries alongside the
  worker's own writes. `accept` runs a plain `git apply`, which fails
  outright when those paths already exist in the working tree — blocking the
  worker's real changes too — so commit or shelve untracked files before
  dispatching against a dirty tree. Non-git targets are unaffected: their
  extraction diffs the directory against the merged view, so untouched files
  never appear.
- A test that has two same-process calls contend on the same
  `withFileLockAsync()`/`withFileLock()` lock path (one holding it across an
  `await`, another trying to acquire it concurrently) hanging or timing out
  at exactly the lock's `timeoutMs` — expected, not a bug in the lock.
  `acquireLock()`'s retry loop blocks the JS thread synchronously via
  `Atomics.wait` with zero yield back to the event loop, so a contending
  same-process caller starves the very continuation that would release the
  lock. Real contention is always cross-process (a separate `taskferry`
  invocation, each with its own event loop), where this doesn't arise —
  see `state-lock.js`'s row above and the note in `state-lock.test.js`.
