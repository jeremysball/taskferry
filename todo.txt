TASKFERRY TODO — MASTER TRACKER
================================

Instructions:
- Mark items complete with [X]
- Denote plan status: (plan) = has plan, (spec) = spec only, (none) = needs design
- If has plan, just write (plan) — it implies spec too
- Keep this file updated before/after each session


TIER 0: BLOCKED — FIX PREMISE BEFORE ANY WORK STARTS
=====================================================

[X] Setup wrapper-script fix (replace broken symlink premise)
    Status: shipped — merged from feat/taskferry-setup (b208bc2)
    Details: fixed at the root instead of routing around it with a wrapper
             script. src/cli.js now resolves process.argv[1] through
             fs.realpathSync before comparing to import.meta.url, so a
             symlinked entrypoint (~/.local/bin/taskferry) resolves and runs
             main() correctly instead of silently exiting 0. Regression test
             spawns the CLI through a real symlink. Also brought in
             src/setup.js (local install bootstrap) and doc updates.

[X] Watchdog two-phase no-output timeout
    Status: shipped — merged from worktree-wait-hang-and-plan-fixes
             (a371a13, b36158d)
    Details: post-output no-output timeout option plumbed through, watchdog
             escalates after the first log event instead of using one flat
             timeout for the whole task lifetime. 127 lines of new tests
             covering the escalation path.


TIER VR: VERY REQUESTED — TOP PRIORITY
=======================================

[_] Failure detail field
    Status: unplanned
    Details: richer error context alongside failureReason; surface from
             wait/status --full/result --full
    Plan: (none) — needs design

[_] Error classification (4-bucket system)
    Status: unplanned
    Details: authentication_failed, payment_required, rate_limited,
             no_output_timeout — only no_output_timeout exists today
    Plan: (none) — needs design

[_] Resume command hints on no_output_timeout crash
    Status: unplanned
    Details: prefill `--session-id` in the `next` hint when a task dies with
             a salvageable sessionId
    Plan: (none) — needs design

[_] Final marker validation
    Status: unplanned
    Details: enforce that a task told to end with `Status:` actually did;
             flip status when the marker is absent
    Plan: (none) — needs design

[_] Empty message handling
    Status: unplanned
    Details: a task can settle `done`/exit 0 with an empty final message —
             detect and flag this instead of reporting clean success
    Plan: (none) — needs design


TIER IMPORTANT: STANDALONE FEATURES
====================================

[_] LLM progress summaries (wait --summarize)
    Status: unplanned
    Details: optional flag on wait/status to condense narration tail into a
             one-line "what's happening now" via a small configurable model
    Plan: (none) — needs design

[_] Tool selection for dispatch
    Status: unplanned
    Details: dispatch always uses `opencode run --auto`; add caller control
             over which tools are accessible to the task runner
    Plan: (none) — needs design

[_] Agent selection for dispatch
    Status: unplanned
    Details: select/reuse/create task-runner agents on the fly, not just the
             fixed internal `taskferry-summary` agent
    Plan: (none) — needs design


TIER DOCKER: BUNDLED INTO FUTURE DEPLOYMENT LAYER
==================================================

[_] Admin UI / key management panel
    Status: deferred — becomes part of Docker deployment, not taskferry core
    Details: key storage/management folded into Docker volume + env config

[X] Wait timeout clamp removal
    Status: shipped — merged from worktree-wait-hang-and-plan-fixes (8cd5201),
             superseding an earlier simpler fix (35287ee) directly on main
    Details: `taskferry wait` with no --timeout-ms now blocks until real
             settlement; passing --timeout-ms still opts into an early
             return at that many ms. maxWaitMs is now an injectable factory
             option (defaults to MAX_WAIT_MS) rather than a hardcoded
             constant, and advisor()'s poll() call site now honors
             timeout_ms too. Tests added in tasks.test.js. Docs fixed in
             docs/cli-reference.md (wait + advisor sections).

[_] Doctor --full environmental output
    Status: deferred — bundled with Docker tier (goes with key management)
    Details: report daemon source path, key-slot names, provider environment
             targets, daemon inheritance


TIER NOT DOING / DREAM
=======================

[_] Polling interval control — not worrying about it, defaults are fine
[_] Task interleaving — dream category, probably never happens
[_] Credential redaction — personal server only, not worth the effort
[_] Brand prototype merge decision — deferred indefinitely (feat/taskferry-brand)


COMPLETED
=========

[X] dispatch-reliability — 8/10 tasks shipped, task 10 verification-only, plan deleted
[X] taskferry-advisor — full implementation on main, plan deleted
[X] taskferry-axi-cli — full implementation on main, plan deleted
[X] Skill generation — `npm run skill:generate` / `npm run skill:check` working
[X] Skill guidance updates — prompt file storage, no_output_timeout crash handling,
    --timeout-ms guidance added to skills/taskferry/SKILL.md and distributed copies


NOTES
=====

- Tier 0 blocks everything downstream in its own plan — fix the premise before
  touching implementation.
- VR tier is the next real priority once Tier 0 is clear.
- Docker tier items (admin UI, wait clamp, doctor --full) are intentionally
  bundled — do not implement them separately in taskferry core.
- Dream tier is not scheduled; revisit only if priorities change.
