{
  "//": "TEMPORARY scaffold for the typecheck-coverage branch. Widens the program to the three real entrypoints (package.json bin, the daemon spawn target, and package.json exports) so the CLI/daemon half of src/ is typechecked while it is being annotated. Deleted in the final commit, which folds this include list into tsconfig.json itself.",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "target": "es2023",
    "lib": ["es2023"],
    "types": ["node"]
  },
  "include": ["src/cli.js", "src/daemon.js", "src/opencode-plugin.js"]
}
